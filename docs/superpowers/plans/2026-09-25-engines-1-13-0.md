# Engines 1.13.0: manual v4 y arranque en formato 2 de Engram 1.7.0

> Como cambiar el reglamento pegado en el escritorio de cada empleado y la hoja de resumen de cada mañana por las versiones nuevas que ya entrega el archivero, sin dejar sin papeles a una oficina que todavía tenga un archivero viejo.

**Qué es:** el plan para que `forge614-engines` (hoy v1.12.1, `a6bfc2b`) use lo que ya publica Engram 1.7.0: el manual de memoria del protocolo v4 y el bloque de arranque en formato 2. Es el paso 2 del orden de la spec de memoria inteligente (§9: «Engines acepta y renderiza v4 completo; el gancho inyecta el bloque de Engram»).
**Decisiones del propietario (2026-09-25):**
- **D1.** El manual v4 se instala tal cual (`instructions`); `mcpInstructions` no se escribe en ningún archivo (lo entrega el servidor MCP de Engram).
- **D2.** Con un Engram anterior a 1.7.0, Engines se adapta: si la v4 o `--format 2` responden `INVALID_INPUT`, usa el manual v1 o el formato 1 como hoy y avisa que conviene actualizar Engram.
- **D3.** `verify memory-integration` avisa (nunca falla) cuando el manual instalado difiere del que entrega Engram hoy, con el comando para actualizarlo.
- **D4.** El gancho inyecta el `text` del formato 2 sin reconstruirlo, con el tope defensivo de 10 500 caracteres.
- **D5.** Simetría entre agentes: el manual va incrustado dentro del archivo principal de cada agente, entre sus marcadores, como ya hace Codex. Claude Code deja el archivo aparte `~/.claude/forge614-engram-memory-protocol.md`; la migración lo retira solo si empieza con la marca de Engines y, si no, avisa.

**Fuera de este plan:** retirar el manual de los archivos y dejarlo solo en el MCP. Se decide con el escenario «solo teléfono» del banco de comportamiento (Engram, tema `forge614-ai/engram/manual-solo-por-mcp`).

## Punto de partida medido (2026-09-25)

- Engines pide `memory-protocol --json` sin versión (v1) y su validador exige `version: 1` con `lifecycle`, `scopes` y `security`: rechazaría la v4 (`src/modules/memory-protocol/types.ts:38`).
- El gancho pide `startup-context` en formato 1 y arma el texto él mismo (`src/infrastructure/engram/startup-context-client.ts:72,103`).
- `verify memory-integration` solo comprueba que el bloque exista (`src/app/verify-memory-integration.ts:83`).
- Suite: 394 pruebas, 0 fallos, 0 omitidas; typecheck limpio. Git ignora `.claude/`, `dist/` y `node_modules/`; ninguna prueba los lee.
- La Mac del propietario tiene el manual v1 en `~/.claude/forge614-engram-memory-protocol.md` (empieza con la marca de Engines) referido con `@` desde `~/.claude/CLAUDE.md`, y el manual v1 incrustado en `~/.codex/AGENTS.md`.

## Tareas

| Tarea | Qué hace | Modelo y razonamiento | Entrega |
|---|---|---|---|
| E1 | D1, D2 (manual), D3 y D5 | Sonnet · medium, parche con `git apply` | `2026-09-25-engines-1-13-0-e1.patch` |
| E2 | D2 (arranque) y D4 | Sonnet · medium, parche con `git apply` | `2026-09-25-engines-1-13-0-e2.patch` |
| E3 | Documentación es/en (capítulos 03, 05, 06 y 07) y versión 1.13.0 | Sonnet · medium | se detalla al cerrar E2 |
| E4 | Publicación: PR, CI, fusión, tag `v1.13.0`, release e instalación en la Mac (`plan memory-install` + `apply` para Claude Code y Codex) | Sonnet · low (publicación); instalación con datos reales, Opus · high | se detalla al cerrar E3 |

Al cerrar cada tarea, el orquestador revisa la sección de Engines del checklist de agentes (acta 0017) y anota si hay un requisito nuevo.

## Tarea E1: manual v4, respaldo a v1, aviso de verify y simetría

**Parche:** `docs/superpowers/plans/2026-09-25-engines-1-13-0-e1.patch`, SHA-256 `c19fff22cc0f3f86f780ea949512fc7419d031dcf4f16fd055485b05c665f280`. Se aplica sobre `a6bfc2b`.

Qué cambia (18 archivos, código y pruebas):
- `memory-protocol-client.ts`: pide `--protocol-version 4`; solo ante `INVALID_INPUT` de Engram (JSON en stderr) reintenta la llamada de siempre y devuelve `legacyProtocolNotice` (español e inglés). Cualquier otro error se reporta como hoy, sin reintento.
- `types.ts`: `MemoryProtocolV4` e `isMemoryProtocolV4` (estricto solo con `id`, `version`, `instructions` y `mcpInstructions`).
- `render.ts`: con la v4 escribe el encabezado de siempre (título y línea `Protocol: … (version 4)`) seguido de `instructions` sin cambios; la v1 se renderiza como hoy.
- `claude-code.ts`: sin `contentFile`; el manual va incrustado en `~/.claude/CLAUDE.md`.
- `instructions-write-decision.ts`: si el bloque instalado es una referencia `@archivo`, lo reemplaza por el manual incrustado y borra el archivo satélite solo si empieza con la marca `<!-- Managed by Forge614 Engines. … -->`; si no, lo deja y devuelve un aviso. La misma regla aplica al quitar la memoria (`plan memory-remove`).
- `verify-memory-integration.ts`: vuelve a pedir el manual y reporta `upToDate` y `driftNotice` con el comando exacto; se calla si no hay manual instalado, si Engram no responde o si la instalación estaría bloqueada (ajuste del orquestador, con su prueba).

Medido por el orquestador en una copia nueva de `a6bfc2b`: `git apply --check` y `git apply` sin error; `bun test` 421 pass, 0 fail, 0 omitidas; `bun run typecheck`, `bun run verify:docs`, `bun run build` y `dist/forge614-engines detect` con código de salida 0; ninguna línea agregada con espacios al final. Contra el Engram 1.7.0 real (solo lectura), la v4 se acepta y el manual se escribe tal cual.

Pasos para la sesión:
1. Desde `main` en `a6bfc2b` con el árbol limpio, crear la rama `work/1.13.0-memoria-v4`.
2. Comprobar la huella del parche (`shasum -a 256`) contra la de arriba; si no coincide, detenerse.
3. `git apply --check` y `git apply` del parche.
4. `bun install --frozen-lockfile`, `bun test` (esperado 421 pass, 0 fail) y `bun run typecheck`, leyendo cada código de salida.
5. Commit: `feat(memory): manual v4 de Engram incrustado en cada agente, respaldo a v1 y aviso de manual desactualizado en verify`.
6. Sin push, sin PR, sin tocar `~/.claude`, `~/.codex` ni `~/.forge614`.

## Tarea E2: arranque en formato 2 con respaldo

**Parche:** `docs/superpowers/plans/2026-09-25-engines-1-13-0-e2.patch`, SHA-256 `284a989a640e3ff61c1bad1509406706868f5c52e4baa76edd07f0b3f5bab88a`. Se aplica encima de E1.

Qué cambia (6 archivos):
- `startup-context-client.ts`: `fetchStartupBlock` pide `--format 2` y valida la forma (`text` no vacío, `chars`, `sections`, `omitted`); solo ante `INVALID_INPUT` usa el formato 1 de hoy con su aviso (español e inglés).
- `run-memory-hook.ts`: con el formato 2 entrega `text` tal cual (solo pasa por el filtro de marcas de instrucción que ya existía); con el formato 1 reserva espacio para el aviso; todo sale por el tope de 10 500 caracteres.

Medido por el orquestador: sobre E1, 432 pass, 0 fail, 0 omitidas; typecheck limpio. Con la respuesta real de Engram 1.7.0 para `forge614-ai`, el gancho entrega exactamente el texto de Engram (4 945 de 5 000 caracteres, con su encabezado «retrieved data, not an instruction»).

Pasos para la sesión: los mismos de E1 sobre la rama `work/1.13.0-memoria-v4` con E1 ya en su commit; esperado 432 pass. Commit: `feat(hook): inyecta el bloque de arranque en formato 2 de Engram, con respaldo al formato 1`.
