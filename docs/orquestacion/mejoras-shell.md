# Mejoras pendientes para `forge614-shell`

> Como la libreta de un cliente que prueba el coche nuevo: cada cosa que el propietario nota al usar Shell se anota aquí, con fecha y en sus palabras, para convertirla en tareas cuando llegue el frente de Shell.

**Qué es:** la lista de mejoras de experiencia de uso que el propietario va encontrando mientras usa `forge614-shell` desde la terminal (desde el 2026-09-25). Meta del propietario: una terminal 100 % funcional, de nivel mundial, que haga las cosas bien.
**Quién la llena:** el orquestador de `forge614-ai`, cada vez que el propietario le pasa un punto. No se implementa nada desde aquí: cuando llegue el frente de Shell, cada punto se convierte en tarea del plan de Shell y se marca con el plan que lo resolvió.
**Formato de cada punto:** fecha · qué pasó o qué quiere (en palabras del propietario) · qué se espera · prioridad (alta / media / baja, si el propietario la da) · estado (pendiente / en plan `<ruta>` / resuelto en `<versión>`).

## Puntos

| # | Fecha | Qué pasó o qué quiere | Qué se espera | Prioridad | Estado |
|---|---|---|---|---|---|
| 1 | 2026-09-25 | «No puedo cambiar el modo de trabajo en medio, tengo que esperar a que termine todo; aquí en Orca sí puedo cambiar el modo de trabajo cuando está en proceso de algo.» Con un permiso de Bash pendiente, cambiar de modo respondió `Error: Termina o usa /stop en el turno actual primero.` | Cambiar el modo de trabajo (manual ↔ otros, `Shift+Tab`) en cualquier momento, también a mitad de un turno o con un permiso pendiente, sin tener que esperar ni usar `/stop`, como en Orca | — | pendiente |
| 2 | 2026-09-25 | «En el sidebar mostrar qué está usando del ecosistema de Forge614: Engram, Engines, etc., como para decirle al usuario qué tiene instalado, qué usa y qué no usa; también los MCP conectados, pero por Forge614.» Hoy la barra lateral (Shell v1.11.0) muestra Sesión, Contexto, Uso del plan y Recursos, sin nada del ecosistema | Una sección de la barra lateral con cada pieza de Forge614 (Engram, Engines, etc.): si está instalada, su versión y si esta sesión la está usando o no; y la lista de servidores MCP conectados a través de Forge614 | — | pendiente |
| 3 | 2026-09-25 | «Esto es importante: al seleccionar no puedo seleccionar solo lo del chat, se pasa al sidebar, como que no funciona bien; y luego doy scroll y se baja la selección en lugar de solo dar scroll sin modificar la selección.» En las capturas, al arrastrar sobre el reporte, el resaltado toma también la barra lateral (Tiempo activo, Contexto, Uso del plan) y, al desplazar, la selección se mueve con la pantalla | Que la selección con el ratón se quede solo dentro del chat, sin tomar la barra lateral, y que desplazar la pantalla no cambie lo ya seleccionado | alta | pendiente |
| 4 | 2026-09-25 | Visto por el orquestador en la captura del punto 1 y aprobado por el propietario para anotarse: el permiso solicitado se muestra como texto técnico crudo, `Permiso solicitado: Bash { "command": "rtk grep …", "description": "…" }` con `/yes` y `/no` | Mostrar el permiso en palabras normales: primero la descripción («Buscar el changelog de Engram en este repositorio»), el comando aparte y legible, y botones o teclas claras para permitir o negar | — | pendiente |
| 5 | 2026-09-25 | Visto por el orquestador en la captura del punto 2 y aprobado por el propietario para anotarse: al abrir la sesión, la barra lateral decía «Contexto: Medición no disponible»; minutos después ya mostraba 17 % (170,6k / 1M tokens) | Que la medición del contexto aparezca desde el inicio de la sesión, o que diga por qué todavía no hay dato (por ejemplo «se mide tras el primer mensaje») | — | pendiente (en una sesión nueva de las 9:57 ya decía «Disponible después de la prime…», pero el texto sale cortado por el ancho de la barra) |
| 6 | 2026-09-25 | «Poderme cambiar entre agentes desde ahí mismo, porque al iniciar seleccionas el modelo, pero para cambiarte de agente tienes que salirte completamente con /quit y luego volver a seleccionar. A lo mejor algo como /agent, pero tienen que ser cosas distintivas de Shell: hay comandos nativos del agente (/model, por ejemplo), y en Shell debemos usar prefijos para distinguir que son comandos de Shell.» Hoy los comandos propios de Shell se mezclan con los nativos sin prefijo (`/quit`, `/status`, `/login`, `/logout`, `/new`, `/resume`, `/refresh`, `/stop`, `/yes`, `/no`, `/help`, `/commands`); ya existe un alias con prefijo, `/forge614-status` (`src/ui/basic/claude.ts:339`) | Un comando de Shell para cambiar de agente (Claude Code ↔ Codex) sin salir, y un prefijo único para todos los comandos de Shell. Opciones propuestas: `/f614:agent` (corto, como la marca «F614» de la barra de estado; al escribir `/f614:` se listan solo los de Shell), `/shell:agent` (legible, pero puede chocar con un plugin llamado «shell», que usa la misma forma `plugin:comando`), `/forge614-agent` (sigue el alias existente `/forge614-status`, más largo) o `:agent` (otro signo, sin barra; nunca choca con los nativos, pero es menos conocido). Decisión final del propietario (2026-09-25): prefijo **`/f614:`** (por ejemplo `/f614:agent`, `/f614:quit`). Primero pidió uno universal sin la marca y eligió `/sh:`; luego lo cambió: «no dejes /sh:, mejor deja /f614:command». Los comandos actuales de Shell pasan a `/f614:`. Precisión del propietario (mismo día, puntos 23 y 6): «ni el de quit tampoco puede ir sin prefijo; absolutamente los únicos que quedan sin prefijo son los nativos de cada agente». **Sin alias**: `/quit`, `/status`, `/login`, `/logout`, `/new`, `/resume`, `/refresh`, `/stop`, `/yes`, `/no`, `/help`, `/commands` y `/forge614-status` de Shell pasan todos a `/f614:<nombre>`, y sin prefijo solo existe lo que es nativo del asistente elegido | alta | pendiente (prefijo decidido: `/f614:`, sin alias) |
| 7 | 2026-09-25 | «Al iniciar una nueva sesión me recuerda el agente y el razonamiento que utilicé la última vez, creo; si no, verifícalo y lo apuntas como mejora. Pero quiero que también recuerde mi modo de trabajar que utilicé la última vez: que me pregunte o que haga todo automático.» Verificado en Shell 1.11.0: `~/.forge614/shell/preferences.json` guarda modelo y razonamiento **por agente** (`claude: sonnet/medium`, `codex: gpt-5.6-terra/medium`; `src/infrastructure/shell-preferences.ts:7-9`), pero **no** guarda qué agente se usó la última vez ni el modo de trabajo: cada sesión arranca en modo manual (`permissionMode = "default"`, `src/engines/claude/session.ts:119`) | Recordar el último agente elegido y, por agente, el último modo de trabajo (manual que pregunta / automático), junto al modelo y razonamiento que ya recuerda | — | pendiente |
| 8 | 2026-09-25 | «El /resume no es nada interactivo: no puedo elegir, no puedo moverle, solo me enumera; está de la mierda esa experiencia.» Hoy `/resume` imprime una lista numerada solo con títulos (varios repetidos, como «Color favorito» tres veces) y pide escribir `/resume <número>` | Un selector interactivo: moverse con las flechas, buscar escribiendo, ver de cada conversación la fecha, la carpeta o el proyecto y el primer mensaje para distinguir las repetidas, Enter para retomarla y Esc para cancelar | — | pendiente |
| 9 | 2026-09-25 | «Otra mejora importante: no me deja abrir diferentes pestañas. Si estoy trabajando en la terminal directamente y quiero abrir el Shell de este orquestador, no puedo: tengo que abrir otro Shell en otra ventana. Hay que ver si las ponemos como tabs en la parte superior o como lo hace Warp, que creo que sería lo mejor.» Referencia que mostró: Warp, con una columna de pestañas a la izquierda (buscador «Search tabs…», botón «+», cada pestaña con su último comando y su carpeta) y la sesión activa a la derecha | Varias sesiones dentro de una misma ventana de Shell, como pestañas: preferencia del propietario, una columna lateral al estilo de Warp (buscar pestañas, crear una nueva con «+», ver en cada una su título o agente y su carpeta, cambiar entre ellas sin cerrar ninguna); la alternativa son pestañas en la parte superior | alta | pendiente |
| 10 | 2026-09-25 | «Mejora visual para Shell: lo de recursos hay que ponerlo abajo, a un lado de la sesión, al igual un conector para los MCP, así como lo hace Orca. Así lo tenemos y así lo tiene Orca; esto estaría increíble así, y que le puedas dar clic y te muestre en un modal o algo lo de cada uno, como este: le di clic al conector, pero ahí salen los MCP o así.» Hoy (Shell v1.11.0) la barra lateral tiene «Recursos» (RAM de Shell 150 MB, RAM del motor «No reportado po…», cortado) y «Actividad en segundo plano». Referencia que mostró: la barra inferior de Orca con indicadores compactos (memoria «14.50 GB», terminales «12», un conector con «6»); al dar clic al conector se abre un panel «Ports» agrupado por proyecto, cada fila con su número, el programa (codex, agy) y la dirección, y botones al pasar el ratón (abrir, copiar, cerrar) | Una barra de estado inferior junto a la sesión con indicadores pequeños: recursos (RAM de Shell y del motor) y un conector con el número de servidores MCP conectados; al dar clic a cada indicador se abre un panel o modal con el detalle (para el conector: cada MCP, su estado y de qué pieza de Forge614 viene). Libera la barra lateral y se relaciona con el punto 2 | — | pendiente |
| 11 | 2026-09-25 | «Mejora visual para Shell: ya no se necesita mostrar el modelo y el razonamiento abajo del chat si eso ya lo tiene el sidebar del lado derecho; es repetir información de a gratis.» Hoy la barra de estado bajo el chat dice «F614 · Claude Code · Sonnet 5 · Low · 1 en segundo plano · ~/Desktop/forge614-engines · work/1.13.0-memoria-v4 · Limpio», y la barra lateral ya muestra Proveedor, Modelo y Razonamiento | Quitar de la barra de estado inferior lo que ya muestra la barra lateral (agente, modelo y razonamiento) y dejar solo lo que no se repite (segundo plano, carpeta, rama, estado de Git). Ese espacio libre puede alojar los indicadores del punto 10 (recursos y conector de MCP) | — | pendiente |
| 12 | 2026-09-25 | «Esto se ve muy mal visualmente en Shell: el chat solo debería ver el "ejecutando" un mensaje, no estarlo llenando.» En la captura, un solo comando largo (esperar los checks del PR) agrega un renglón nuevo cada 30 s: «Bash └ Ejecutando · 240s», «… 270s», «… 300s» … hasta «450s» | Un comando en curso ocupa un solo renglón en el chat que se actualiza en su lugar (el tiempo corre ahí mismo) y queda fijo con su resultado al terminar | — | pendiente |
| 13 | 2026-09-25 | «En donde dice Trabajando y el tiempo no solo segundos: deben salir 1M 27S y horas también, porque todo es contador.» Hoy dice «Trabajando · 503s» y «Ejecutando · 450s» | Todo contador de tiempo en formato legible: `45s`, `1m 27s`, `8m 23s`, `1h 02m`, en el indicador «Trabajando», en cada herramienta y en la barra lateral | — | pendiente |
| 14 | 2026-09-25 | «Ahora no dice en qué está trabajando; por ejemplo con Orca ya sé que está trabajando en el workflow, pero en el Shell no. Hay que mostrar algo para saber, porque solo dice trabajando, pero ¿en qué o qué? Ah, sí, ya vi dónde lo dice, pero no sé, hay que mejorarlo.» Referencias: Orca muestra en su lista cada proyecto con su rama, un punto de estado, el PR ligado y la tarea en curso; Shell solo dice «Trabajando · 503s», y lo que hace está en la barra lateral, «Actividad en segundo plano», con cada tarea cortada y repetida en dos renglones («Aplicar parche y correr veri… └ Aplicar parche y correr ve…») | Junto a «Trabajando» y su tiempo, una frase corta de lo que hace ahora (la descripción del comando o del paso, por ejemplo «Esperando los checks del PR #3 · 8m 23s»); en la barra lateral, cada tarea en segundo plano en un solo renglón legible, sin repetir el título, con su estado y su tiempo | — | pendiente |
| 15 | 2026-09-25 | «Los mensajes en gris están hasta la orilla, topan; no están alineados a donde dice agente o el mensaje del agente o el propio de hecho; deberían estar más adentro para que se entienda que es una subtarea de lo que se mandó o de lo que respondió el agente.» En la captura, cada herramienta («• Bash · Completado · 17.7s └ merge=0») empieza pegada al borde izquierdo del chat, más afuera que «ASISTENTE · 11:55 a.m.» y su texto | Las herramientas (renglones grises) con sangría bajo el mensaje del asistente al que pertenecen, más adentro que el encabezado y el texto, para que se lean como subtareas de ese mensaje | — | pendiente |
| 16 | 2026-09-25 | «Ahí también mandó links, o parecen, pero no les puedo dar clic ni abrirlos; eso hay que mejorarlo también.» En la captura del reporte de Engines, las direcciones del PR y de la release salen subrayadas como enlace, pero no responden al clic | Que toda dirección web del chat (y las rutas de archivos) se pueda abrir con clic o Cmd+clic, como en una terminal moderna (enlaces de terminal OSC 8), y que solo se vea como enlace lo que de verdad se puede abrir | — | pendiente |
| 17 | 2026-09-25 | «Que pueda arrastrar el sidebar, hacerlo más grande o más pequeño, como en Orca, que donde se muestran los archivos es un panel configurable: puedo agrandarlo o achicarlo; obviamente tiene que tener un mínimo de chico.» Grabó un video de 10 s (`~/Desktop/Grabación de pantalla 2026-09-25 a la(s) 12.00.42 p.m..mov`): en Orca, al pasar el ratón por el borde entre el chat y el panel derecho, el cursor cambia a flechas de ajuste y, al arrastrar, el panel se ensancha o se angosta y el chat se acomoda. Hoy la barra lateral de Shell tiene ancho fijo y corta los textos («RAM del motor No reportado po…», «Último turno 2200 entrada · 5…») | Barra lateral con ancho ajustable arrastrando su borde con el ratón, con un ancho mínimo (y uno máximo) para que no se rompa, y que recuerde el ancho elegido entre sesiones (como el punto 7 con las demás preferencias); con más ancho, los textos cortados se ven completos | — | pendiente |
| 18 | 2026-09-25 | «Esto es muy bueno, lo que dice de Precipitating.» Referencia: el indicador de trabajo de Claude Code en la terminal dice «✻ Precipitating… (3m 13s · ↓ 13.8k tokens)»: un verbo que cambia en cada turno, el tiempo en minutos y segundos y los tokens que van llegando; Shell dice solo «Trabajando · 503s» | Un indicador de trabajo con más vida e información: verbo o frase que varía (con la voz de Forge614), tiempo legible (punto 13), tokens recibidos en vivo y, junto, qué está haciendo (punto 14) | — | pendiente |
| 19 | 2026-09-25 | «Esto no se entiende absolutamente nada de Shell, ¿qué es? Hay que mejorarlo visualmente muchísimo.» En la barra lateral, bajo «Uso del plan»: «Último turno 2996 entrada · 7…», «Costo estimado de API $0.6723», «Solo de referencia, no se fact…» (cortado) | Explicarlo en palabras normales y sin cortes, por ejemplo: «Este mensaje: leyó 2 996 tokens y escribió 7…»; «Si pagaras por uso costaría ≈ $0,67 (tu plan no lo cobra)»; con título propio o dentro de una sección clara, y el detalle técnico al pasar el ratón o al abrirlo (ligado a los puntos 10 y 17) | — | pendiente |
| 20 | 2026-09-25 | «Luego abajo del chat también sale ctx 6%, que es contexto, pero eso ya lo tiene el sidebar con gráfica; también hay que quitarlo de abajo del chat.» | Quitar «ctx» de la barra inferior porque la barra lateral ya lo muestra con su gráfica; misma regla que el punto 11: la barra inferior no repite nada de la lateral | — | pendiente |
| 21 | 2026-09-25 | «¿Es posible que Shell te mande notificaciones cuando termina algo? Así manda, por ejemplo, Orca.» Referencia que mostró: un aviso de macOS de Orca con su icono, el título «forge614 / main - Claude finished» y las primeras líneas de la respuesta final («## Hecho **T6 hecha y marcada en el plan. Me detengo a la espera de la revisión.** …»). Verificado en Shell 1.11.0: no envía avisos al escritorio | Un aviso del sistema cuando termina un turno, cuando el asistente pide un permiso o se detiene esperando al propietario, y cuando falla algo: título con la carpeta o proyecto, la rama y qué pasó («Claude Code terminó», «Codex pide permiso»), y de cuerpo las primeras líneas de la respuesta; al dar clic, vuelve a la pestaña de esa sesión (ligado al punto 9). Solo cuando la ventana de Shell no está al frente, y con opción para apagarlo. Formas posibles: la secuencia de aviso de la terminal (OSC 9 o 777, que muestran Ghostty, iTerm2 o WezTerm) o el aviso nativo de macOS | — | pendiente |
| 22 | 2026-09-25 | «Ahora Shell tiene como un diseño muy de bordes y así, y a mí no me gusta; me gustan más fondos sin bordes con contraste al tema. Por ejemplo, este dashboard es mío y me encanta; quiero algo así: que se rediseñe Shell con ese estilo.» Referencia que mostró: su panel «Forge614 Admin» en modo oscuro: fondo casi negro; cada zona (barra lateral, tarjetas de números, filas de tablas, filas de actividad) es un bloque de fondo un poco más claro, con esquinas suaves y **sin ninguna línea de borde**; la separación sale solo del contraste de fondos y del espacio; textos secundarios en gris apagado; el estado con un punto verde pequeño («● Activa»); iconos dentro de cuadritos de color suave (verde para éxito, rojo o ámbar para fallos y avisos); la opción activa del menú, resaltada con un fondo más claro y una rayita a la izquierda; tiene modo claro y oscuro. Verificado en Shell 1.11.0: dibuja cajas con caracteres de línea (`╭─╮│`) en `src/ui/basic/workspace.ts`, `composer.ts`, `transcript.ts`, `sidebar.ts`, `theme.ts` y en el arranque (`src/ui/startup/frame.ts`, `group-picker.ts`); ya existe un fondo de panel azulado en `theme.ts:13` (`48;2;20;31;39`) | Rediseño visual completo de Shell con el estilo de ese panel: sin bordes de línea; barra lateral, cuadro para escribir, tarjetas de herramientas y ventanas de selección como bloques de fondo que contrastan con el fondo de la terminal; grises neutros en lugar del azulado de hoy; jerarquía con tamaño o brillo del texto y color apagado para lo secundario; estados con punto de color e iconos en cuadritos de color suave. **Oscuro siempre** (aclaración del propietario, mismo día: «Me gusta una UI limpia, pero que sea dark, eso sí», mostrando la vista de usuario del mismo panel en modo claro como ejemplo de limpieza): mucho espacio libre y pocos elementos; pestañas como una píldora con fondo más claro para la activa; buscador y filtros («Resultado Todos», «Orden Más recientes») como bloques rellenos sin borde; cada fila, una tarjeta rellena con la fecha en gris a la izquierda, el evento en letra de código y el resultado a la derecha como punto de color con texto («● Denegado» en ámbar, «● Correcto» en verde); la acción peligrosa («Desactivar»), en rojo suave sobre fondo rojizo. Se hace junto con los puntos visuales (10, 11, 15, 17, 19 y 20) para no rediseñar dos veces | — | pendiente |
| 23 | 2026-09-25 | «En Codex no funciona /compact.» En la captura (Shell 1.11.0 con Codex, GPT-5.6-Terra): tras «hola», `/compact` respondió `Error: Comando desconocido: /compact. Usa /help.`; con Claude Code, `/compact` sí funcionó en Shell el mismo día y la memoria se volvió a cargar. Verificado en el código: Shell no tiene ningún `compact` para Codex (`src/engines/codex/session.ts`, `src/app/native-chat.ts`) | `/compact` (compactar: resumir la conversación para liberar espacio) funciona igual con Codex que con Claude Code, pidiéndoselo al motor de Codex, y después de compactar la memoria de Engram se vuelve a cargar; en general, **regla del propietario** (mismo día): «se supone que todos los comandos son los nativos de cada agente, y si es uno propio de Shell debe ser con `/f614:`; en Codex normal sí funciona `/compact` y en Shell no». Es decir: todo `/comando` sin prefijo es el comando nativo del asistente elegido y se comporta como en su terminal normal; solo los de Shell llevan `/f614:`. Con Claude Code, Shell ya pasa los comandos al motor (por eso `/compact` funcionó); con Codex, Shell habla con su servidor (`app-server`) y no con su pantalla, así que cada comando nativo se traduce a la petición equivalente del servidor, y si alguno no existe ahí, Shell lo dice con esas palabras («Codex no permite `/x` desde Shell») en lugar de «Comando desconocido». Confirmado el mismo día en Codex directo: `/compact` compacta y el gancho de Engines vuelve a cargar la memoria en el siguiente turno (registro 19:39:29Z compactado, 19:41:12Z bloque inyectado) | alta | pendiente |
| 24 | 2026-09-25 | Visto en la misma captura del punto 23: al pedir permiso para un comando, Codex en Shell muestra en el chat el mensaje técnico crudo completo (JSON con `"type": "commandExecution"`, `"id"`, `"pluginId": null`, `"cwd"`, `"commandActions"`, `"aggregatedOutput": null`, …). Verificado en el código: `src/engines/codex/session.ts:325` arma el permiso con `JSON.stringify` de todo el evento | Lo mismo que pide el punto 4, también para Codex: el permiso en palabras normales (qué quiere hacer y en qué carpeta), el comando aparte y legible, y nada del JSON interno | — | pendiente |
| 25 | 2026-09-25 | «Mejora visual para Shell: cuando pregunte, como aquí, deben aparecer únicamente Sí o No, o si es en inglés Yes o No, sin el slash (/); y aparte el default debe ser Sí, para que solamente se le dé Enter sin tener que moverte.» En la captura (Codex en Shell 1.11.0, desde `~`): arriba el JSON crudo del permiso (punto 24) y abajo «Permiso · /yes permitir una vez · /no denegar · /stop cancelar turno», con las opciones «› 1. /no Denegar» (marcada) y «2. /yes Permitir solo esta llamada», y el mismo pie repetido. Verificado en el código: el orden `/no` primero y `/yes` después está fijo en `src/ui/basic/native.ts:137` (Codex) y `src/ui/basic/claude.ts:230` (Claude Code); los textos en `src/i18n/es.ts:269-271` | Al pedir permiso, solo dos opciones con palabras: **Sí** y **No** (en inglés **Yes** y **No**), sin barras ni números; **Sí** primero y marcado, para aprobar con solo Enter; No con una tecla o flecha; el pie sin repetirse. Cancelar el turno queda como `/f614:stop` (punto 6). Nota del orquestador: con Sí por defecto, un Enter de más aprueba un comando; el propietario lo prefiere así y el modo manual lo sigue mostrando antes de ejecutar | — | pendiente |
| 26 | 2026-09-25 | «Moví el modo de trabajo y me salió ese error; me salí, volví a entrar, no le moví a nada y funcionó. Los modos de trabajo deben ser nativos del agente también, no de la terminal.» En la captura (Codex en Shell 1.11.0, desde `~/Desktop`, «modo automático activado · escritura en el espacio de trabajo»): `Turno detenido: Invalid request: unknown variant unlessTrusted, expected one of untrusted, on-request, granular, never`. **Es un fallo, no solo una mejora.** Causa verificada en el código: si Codex no devuelve la lista de modos permitidos, Shell usa una lista escrita a mano con nombres viejos (`src/engines/codex/session.ts:98`: `["onRequest", "unlessTrusted"]` y `["readOnly", "workspaceWrite"]`); Codex 0.157.0 ya no acepta `unlessTrusted` (sus valores son `untrusted`, `on-request`, `granular`, `never`), así que el modo automático de Shell con Codex rompe el turno | Los modos de trabajo son los **nativos de cada asistente**, con sus nombres y su comportamiento reales, leídos del propio asistente y nunca de una lista escrita a mano en Shell (misma regla que los comandos, puntos 6 y 23); cambiar de modo nunca rompe el turno; si el asistente rechaza un modo, Shell lo dice en palabras normales y vuelve al modo anterior. Ligado a los puntos 1 (cambiar de modo a mitad de turno) y 7 (recordar el último modo) | alta | pendiente |
