# Reglamento 1.1.2 — Engram de Claude Code y Codex revalidados con 1.7.2 y checklist de sesiones alineado (plan de implementación)

**Goal:** publicar el reglamento (`standard/VERSION`) 1.1.2 con el acta 0029: `engram/claude-code` y
`engram/codex` en `supported` (vencían el 2026-10-25), con la evidencia de Engram 1.7.2 (manual v4 con la
regla 3 nueva y el campo `sessionNotice`); el checklist `forge614-engram` alineado con esa conducta ya
verificada y la tabla «Agentes ya evaluados» alineada con la matriz.

**Origen:** tarea R4 de `docs/superpowers/plans/2026-09-26-engram-1-7-2.md` («Impacto en el procedimiento
de agentes», cambios 4–6) y de `docs/superpowers/plans/2026-09-25-engram-1-7-1.md` (cambios 1–3), con la
evidencia de `docs/orquestacion/revalidaciones/2026-09-25-claude-code-codex.md` (secciones «T4 · Engram
1.7.1» y «T4b · Engram 1.7.2»). Con este plan se cierran las dos últimas celdas que el acta 0027 había
dejado en `revalidate`; ninguna celda de la matriz queda abierta después de este reglamento.

**Dónde se ejecuta:** `~/Desktop/forge614-ai`, rama nueva `standard/reglamento-1.1.2` creada desde
`engram/1-7-2-plan` en el commit que agrega este plan y su parche. `main` = `origin/main` = `1d67765`
(reglamento 1.1.1 publicado). Un solo PR lleva todo (solo documentación). Una tarea = una sesión nueva,
etiqueta `[Reglamento · R<n>]`. Patrón: `docs/superpowers/plans/2026-09-25-reglamento-1-1-1.md`.

## Global Constraints

- Todo comando con `rtk` delante.
- Commits convencionales en español, **sin líneas de atribución ni menciones a ninguna IA**.
- Solo agregar o corregir (acta 0024): ninguna regla, punto, campo ni celda se quita.
- Sin productos prohibidos (`standard/forbidden-mentions.json`); documentación es/en con el mismo
  contenido.
- `CHANGELOG.md` no se edita a mano.
- Generados, nunca a mano: `docs/decisions/INDEX.json` (`decisions:index`), la huella de
  `forge614.node.json` (`standard:pack -- --update-pointer`, que **no** cambia la versión del puntero: esa
  se cambia a mano), huellas de `docs/notion-map.json` (`notion-map:build`). Orden: `decisions:index` antes
  de `standard:pack`.
- `dist/` está ignorado por Git: antes de `verify` se regenera con `bun run standard:pack` (lección de
  1.1.0).

## Tabla de tareas

| # | Tarea | Herramienta · modelo · razonamiento | Variable |
|---|---|---|---|
| R1 | Aplicar el parche, verificar y dos commits (sin push) | Claude Code · Sonnet 5 · medium | parche completo con `git apply` |
| R2 | Push, PR, CI, fusión, tag `standard-v1.1.2` y release | Claude Code · Sonnet 5 · low | publicación (se detalla tras aprobar R1) |

## Qué cambia (contenido del parche)

Parche `2026-09-26-reglamento-1-1-2.patch` (junto a este plan), SHA-256
`f88e9b4f0433f9dca4a1302fb59c924bdf9fbba348f6e7d0410eac248ccea72e`, 382 líneas, 13 archivos (185
inserciones, 41 borrados):

1. **Acta 0029**
   `docs/decisions/0029-revalidacion-de-engram-en-claude-code-y-codex-con-1-7-2.md` y su entrada en
   `docs/decisions/INDEX.json`: cierra `engram/claude-code` y `engram/codex`, resuelve que «ofrecer
   continuar desde su resumen» es conducta esperada pero no obligatoria para certificar (queda como límite
   documentado si el asistente no la hace), y que una pregunta de coordinación en la prueba de sesión en
   paralelo no es falla.
2. **Matriz** `standard/support-matrix.json`: las dos celdas de Engram de Claude Code y Codex pasan de
   `revalidate` a `supported` (`verifiedAt` 2026-09-26, sin `revalidateSince`, `reason` ni `deadline`, frase
   de evidencia en `notes`; la de Codex agrega el límite de «ofrece continuar»). Ninguna otra celda cambia.
3. **Checklist** `standard/procedures/new-agent-checklist.md`, sección `forge614-engram`:
   - El punto de sesión que quedó abierta ya no exige un proyecto sin otras sesiones abiertas; para que la
     segunda reciba `previous` la primera debe llevar más de 30 minutos sin actividad en Engram, la
     verificación agrega `sessionNotice` («was left open; its last activity was at») y aclara que ofrecer
     continuar desde el resumen es lo esperado pero, si el agente no lo hace, se anota como límite sin
     bloquear.
   - Punto nuevo: abrir dos sesiones seguidas y verificar que la segunda recibe `parallel` (con
     `sessionNotice` «Another session is open now: `<id>`.») y que el agente lo dice sin llamarla
     interrumpida ni abandonada y sin retomar el trabajo por su cuenta; preguntarle a la persona antes de
     tocar ese trabajo no es falla.
   - El punto del bloque de arranque (formato 2): la sección «Previous session (interrupted)» aparece tras
     30 minutos de inactividad (se quita la frase sobre «otra sesión ya marcó la cortada»); el texto dice
     «was left open; its last activity was at», igual que `sessionNotice`.
   - El punto de `similar` no cambia de texto: ya pedía explicar a la persona por qué se deja aparte el
     parecido.
   - Nota nueva: las pruebas de conducta de esta sección no limitan el largo de la respuesta pedida al
     agente.
   - Tabla «Agentes ya evaluados»: Claude Code y Codex quedan con Engram en ✅ (revalidado 2026-09-26,
     Engram 1.7.2); la fila de Codex agrega el límite de «ofrece continuar». Párrafo «Revalidaciones
     abiertas» corregido: no quedan celdas abiertas.
4. **Versión 1.1.1 → 1.1.2:** `standard/VERSION`, títulos de `STANDARD.md`/`.en.md`, documentos 00 y 01 es/en
   (incluido `standard-1.1.2.tar.gz`), `forge614.node.json` (versión y huella) y `docs/notion-map.json`.

**Candado 3 (`STANDARD.md` §12):** la sección `forge614-engram` sí cambia de forma sustantiva (los dos
puntos de sesión y `sessionNotice`), que es lo que mantenía en `revalidate` las celdas de Engram desde el
acta 0027; este acta las cierra porque T4 (2026-09-25) y T4b (2026-09-26) ya ejecutaron la validación nueva
contra ese mismo texto y la registran con fecha, en los dos asistentes. Ninguna otra sección de nodo cambia,
así que Engines, Workers, Atlas y Shell no se reabren.

**Laboratorio del orquestador (2026-09-26):** parche aplicado y verificado en una copia limpia de
`46524b8` (`engram/1-7-2-plan`), y comprobado byte a byte en una segunda copia con `git apply --check` +
`git apply` + `diff -r`. Línea base y con el parche, en las dos copias: `bun install --frozen-lockfile
--ignore-scripts` limpio; `bun run decisions:index` → `records: 29`; `bun run standard:pack --
--update-pointer` → huella del puntero `16c3c8377ec7881364d3c77fb3c5a98073a4de7cbf337c03e5567a57e7819327`
(paquete `standard-1.1.2.tar.gz`, `tarSha256`
`d0fd4e62ea9a50ef1f02ee2df9c2bc41c2fc96faa7788cdc2df63af1df72fa05`); `bun run notion-map:build` →
`pages: 6`; `bun run verify` exit 0 con `"standard":"1.1.2"` y `"verdict":"pass"` en las 12 reglas; `bun run
standard:check` exit 0; `bun test` **322 pass / 2 skip / 0 fail** (58 archivos, 1172 `expect()`, las 2
omitidas son de PowerShell, igual que en 1.1.1); `bun run typecheck` exit 0; `git diff --check` vacío;
`git status --short` con exactamente los 13 archivos del parche (`dist/` no aparece, está ignorado). En la
segunda copia, `git apply --check` y `git apply` sin conflictos, la misma huella del puntero, `verify` y
`standard:check` exit 0, y `diff -r` (sin `node_modules` ni `dist`) idéntico a la primera copia.

## R1 — aplicar y verificar

Estado esperado: rama `engram/1-7-2-plan` en el commit que agrega este plan y el parche, árbol limpio.

1. `rtk git switch -c standard/reglamento-1.1.2`.
2. `rtk proxy shasum -a 256 docs/superpowers/plans/2026-09-26-reglamento-1-1-2.patch` =
   `f88e9b4f0433f9dca4a1302fb59c924bdf9fbba348f6e7d0410eac248ccea72e`; `rtk git apply --check` y `rtk git
   apply` del parche.
3. `rtk bun run standard:pack` (regenera `dist/`, ignorado por Git; no debe cambiar ningún archivo
   versionado).
4. Verificación (código de salida de cada una por separado): `rtk bun run verify` 0 con
   `"standard":"1.1.2"`; `rtk bun run standard:check` 0; `rtk bun test` 322 pass / 2 skip / 0 fail; `rtk bun
   run typecheck` 0; `rtk git diff --check` vacío; `rtk git status --short` muestra exactamente los 13
   archivos del parche.
5. Commit 1: `rtk git add docs/decisions/0029-revalidacion-de-engram-en-claude-code-y-codex-con-1-7-2.md
   docs/decisions/INDEX.json && rtk git commit -m "docs(decisions): acta 0029, revalidación de Engram en
   Claude Code y Codex con 1.7.2"`.
6. Commit 2: `rtk git add -A && rtk git commit -m "feat(standard): reglamento 1.1.2 con Engram de Claude
   Code y Codex en supported y el checklist de sesiones alineado"`.
7. Sin push, sin PR y sin tag (R2).
8. Si algo no cuadra: detenerse y reportar; no corregir el parche a mano.

## R2 — publicar

Claude Code · Sonnet 5 · low, sesión nueva en `forge614-ai`, etiqueta `[Reglamento · R2]`. Estado esperado:
rama `standard/reglamento-1.1.2` en el commit que agrega esta sección, árbol limpio; `origin/main` =
`1d67765`. Referencia medida: 1.1.1 fue el PR #13 (checks `verify` y `parity` en ubuntu-24.04, macos-15 y
windows-2025), fusionado con rebase, tag **ligero** `standard-v1.1.1` sobre el commit de `main`, y el
workflow `standard-release` (tags `standard-v*`) publicó `standard-1.1.1.tar.gz`, `SHA256SUMS` y
`pack-manifest.json`. `main` no tiene protección de rama.
1. `rtk git push -u origin standard/reglamento-1.1.2`.
2. `rtk gh pr create --base main --head standard/reglamento-1.1.2 --title "feat(standard): reglamento 1.1.2
   con Engram de Claude Code y Codex en supported"` con un cuerpo corto en español: acta 0029;
   `engram/claude-code` y `engram/codex` en `supported` (revalidados el 2026-09-26 con Engram 1.7.2: manual
   v4 con la regla 3 nueva y `sessionNotice`); no queda ninguna celda en `revalidate`; el límite documentado
   de Codex (no ofrece por su cuenta continuar desde el resumen de una sesión que quedó abierta); el
   checklist alineado (30 minutos de inactividad para `previous`, prueba nueva de `parallel`, `sessionNotice`
   en el bloque de arranque, las pruebas de conducta no limitan el largo de la respuesta) y la tabla
   alineada; versión 1.1.2. Sin líneas de atribución ni menciones a ninguna IA.
3. `rtk gh pr checks <n> --watch`: `verify` y los tres `parity` en verde. Si hay una revisión externa (CodeRabbit) procesando, esperar a que termine y reportar sus hallazgos antes de fusionar; no fusionar con revisiones pendientes (lección de T2 de Engram 1.7.2). Si algo falla, detenerse y
   reportar el nombre del trabajo y las últimas líneas del error; no corregir.
4. `rtk gh pr merge <n> --rebase`; luego `rtk git switch main && rtk git pull --ff-only`; `rtk proxy git
   diff standard/reglamento-1.1.2 main` debe salir vacío.
5. `rtk git tag standard-v1.1.2` (ligero) sobre `main` y `rtk git push origin standard-v1.1.2`.
6. `rtk gh run watch` del workflow `standard-release` de ese tag hasta terminar en verde; `rtk gh release
   view standard-v1.1.2 --json assets --jq '.assets[].name'` lista los 3 archivos. Descargar en una carpeta
   temporal con `rtk gh release download standard-v1.1.2 --dir <temporal>` y comprobar `rtk proxy shasum -a
   256 -c SHA256SUMS` = OK.
7. No tocar ningún otro repositorio ni `~/.forge614`.

## Impacto en el procedimiento de agentes

**Sí.** Este plan publica los cambios acumulados del checklist de agentes en la sección `forge614-engram`
(30 minutos de inactividad para `previous`, prueba de `parallel`, `sessionNotice`, nota de largo de
respuesta) y cierra la revalidación de `engram/claude-code` y `engram/codex`. Con este reglamento, la
matriz de soporte queda sin ninguna celda en `revalidate`.
