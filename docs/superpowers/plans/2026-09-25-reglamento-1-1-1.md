# Reglamento 1.1.1 — Claude Code y Codex revalidados y aclaraciones del checklist (plan de implementación)

**Goal:** publicar el reglamento (`standard/VERSION`) 1.1.1 con el acta 0028: `engines/claude-code`, `engines/codex`, `shell/claude-code` y `shell/codex` en `supported` (vencían el 2026-10-22), la tabla «Agentes ya evaluados» del checklist alineada con la matriz y los cambios acumulados del checklist.

**Origen:** tarea R3 de `docs/superpowers/plans/2026-09-25-revalidacion-matriz-claude-code-codex.md` (cierres de R1, R1b y R2, «Orden decidido»), con la evidencia de `docs/orquestacion/revalidaciones/2026-09-25-claude-code-codex.md`. `engram/claude-code` y `engram/codex` siguen en `revalidate` hasta Engram 1.7.1 (P8′ de Codex), plazo 2026-10-25.

**Dónde se ejecuta:** `~/Desktop/forge614-ai`, rama nueva `standard/reglamento-1.1.1` creada desde `orquestacion/cierre-reglamento-1-1-0` en el commit que agrega este plan y su parche. `main` = `origin/main` = `6c79277`. Un solo PR lleva todo (solo documentación). Una tarea = una sesión nueva, etiqueta `[Reglamento · R<n>]`. Patrón: `docs/superpowers/plans/2026-09-25-reglamento-1-1-0.md`.

## Global Constraints

- Todo comando con `rtk` delante.
- Commits convencionales en español, **sin líneas de atribución ni menciones a ninguna IA**.
- Solo agregar o corregir (acta 0024): ninguna regla, punto, campo ni celda se quita.
- Sin productos prohibidos (`standard/forbidden-mentions.json`); documentación es/en con el mismo contenido.
- `CHANGELOG.md` no se edita a mano.
- Generados, nunca a mano: `docs/decisions/INDEX.json` (`decisions:index`), la huella de `forge614.node.json` (`standard:pack -- --update-pointer`, que **no** cambia la versión del puntero: esa se cambia a mano), huellas de `docs/notion-map.json` (`notion-map:build`). Orden: `decisions:index` antes de `standard:pack`.
- `dist/` está ignorado por Git: antes de `verify` se regenera con `bun run standard:pack` (lección de 1.1.0).

## Tabla de tareas

| # | Tarea | Herramienta · modelo · razonamiento | Variable |
|---|---|---|---|
| R1 | Aplicar el parche, verificar y dos commits (sin push) | Claude Code · Sonnet 5 · medium | parche completo con `git apply` |
| R2 | Push, PR, CI, fusión, tag `standard-v1.1.1` y release | Claude Code · Sonnet 5 · low | publicación (se detalla tras aprobar R1) |

## Qué cambia (contenido del parche)

Parche `2026-09-25-reglamento-1-1-1.patch` (junto a este plan), SHA-256 `e529f62e2e0b430b2d751a2d98e76c15e037abc9d18ca54453d4bc77968106fb`, 505 líneas, 13 archivos (233 inserciones, 56 borrados):

1. **Acta 0028** `docs/decisions/0028-revalidacion-de-claude-code-y-codex-y-aclaraciones-del-checklist.md` y su entrada en `docs/decisions/INDEX.json`.
2. **Matriz** `standard/support-matrix.json`: las cuatro celdas de Engines y Shell de Claude Code y Codex en `supported` (`verifiedAt` 2026-09-25, sin `revalidateSince`, `reason` ni `deadline`, frase de evidencia en `notes`); las dos de Engram siguen en `revalidate` con una frase de resultado en `notes`. Cursor sin cambios.
3. **Checklist** `standard/procedures/new-agent-checklist.md` (las etiquetas B* solo están aquí y en el acta):
   - B1 (Engines): la forma normal es el manual incrustado en el archivo principal del agente; `contentFile` satélite como excepción.
   - B2 (Engines) y B3 (Shell): revalidación del 2026-09-22 ejecutada el 2026-09-25.
   - B4 (Engram, formato 2): frase de «Previous session (interrupted)» corregida y verificación por la transcripción del asistente, no por la evidencia del gancho.
   - B5: la prueba de sesión interrumpida exige un proyecto sin otras sesiones abiertas.
   - B6: manual v4 incrustado como forma normal y definición de «byte a byte».
   - B7: Claude Code toma el manual de las instrucciones del servidor MCP; Codex no, y para él el manual completo es obligatorio.
   - B8: dos caminos válidos ante un secreto; cadena de conexión inventada en la prueba.
   - B9: `similar` solo sin `topicKey` y con semejanza ≥ 0,25; dos guardados en turnos separados.
   - B10: bloque «Cómo probar sin tocar la memoria real (laboratorio, 2026-09-25)».
   - Tabla «Agentes ya evaluados» alineada con la matriz; la fila de Cursor vuelve dentro de la tabla; párrafo «Revalidaciones abiertas» corregido.
4. **Versión 1.1.0 → 1.1.1:** `standard/VERSION`, títulos de `STANDARD.md`/`.en.md`, documentos 00 y 01 es/en (incluido `standard-1.1.1.tar.gz`), `forge614.node.json` (versión y huella) y `docs/notion-map.json`. Las menciones de 1.1.0 en el acta 0027 son históricas y no cambian.

**Candado 3 (`STANDARD.md` §12):** las secciones de Engines y Shell cambian solo para anotar con fecha la revalidación ya ejecutada y aclarar la forma normal del manual (B1–B3), sin validación nueva; las cuatro celdas se validaron el mismo día contra ese texto (acta 0028, punto 4).

**Laboratorio del orquestador (2026-09-25):** borrador y laboratorio de un subagente (Sonnet), revisado línea por línea y rehecho en una copia limpia de `e4ec550`. Línea base: `verify` 0, suite 322 pass / 2 skip / 0 fail, typecheck 0. Con el parche: `verify` 0 (`"standard":"1.1.1"`), `standard:check` 0, suite **322 / 2 / 0** (las 2 omitidas son de PowerShell), typecheck 0, `git diff --check` limpio, 0 términos prohibidos; `git apply --check` y `git apply` en otra copia nueva, `standard:pack`, `verify` 0, `standard:check` 0 y `diff -r` idéntico. Errores del borrador corregidos: la versión del puntero quedaba en 1.1.0 (`--update-pointer` solo cambia la huella, y `verify` no lo detecta), el acta ubicaba mal B1–B3 y decía que el comando fijaba la versión, B7 afirmaba más de lo medido y la receta decía «antes de cualquier guarda» en vez de «antes de cualquier prueba».

## R1 — aplicar y verificar

Estado esperado: rama `orquestacion/cierre-reglamento-1-1-0` en el commit que agrega este plan y el parche, árbol limpio.

1. `rtk git switch -c standard/reglamento-1.1.1`.
2. `rtk proxy shasum -a 256 docs/superpowers/plans/2026-09-25-reglamento-1-1-1.patch` = el SHA-256 de arriba; `rtk git apply --check` y `rtk git apply` del parche.
3. `rtk bun run standard:pack` (regenera `dist/`, ignorado por Git; no debe cambiar ningún archivo versionado).
4. Verificación (código de salida de cada una por separado): `rtk bun run verify` 0 con `"standard":"1.1.1"`; `rtk bun run standard:check` 0; `rtk bun test` 322 pass / 2 skip / 0 fail; `rtk bun run typecheck` 0; `rtk git diff --check` vacío; `rtk git status --short` muestra exactamente los 13 archivos del parche.
5. Commit 1: `rtk git add docs/decisions/0028-revalidacion-de-claude-code-y-codex-y-aclaraciones-del-checklist.md docs/decisions/INDEX.json && rtk git commit -m "docs(decisions): acta 0028, revalidación de Claude Code y Codex y aclaraciones del checklist"`.
6. Commit 2: `rtk git add -A && rtk git commit -m "feat(standard): reglamento 1.1.1 con Engines y Shell de Claude Code y Codex en supported y el checklist aclarado"`.
7. Sin push, sin PR y sin tag (R2).
8. Si algo no cuadra: detenerse y reportar; no corregir el parche a mano.

## Impacto en el procedimiento de agentes

**Sí.** Este plan publica los cambios acumulados del checklist de agentes (B1–B10 y la tabla) y cierra la revalidación de `engines/claude-code`, `engines/codex`, `shell/claude-code` y `shell/codex`. `engram/claude-code` y `engram/codex` siguen en `revalidate` hasta Engram 1.7.1, plazo 2026-10-25.
