# Engram 1.7.1 — sesiones en paralelo según la hora y manual que explica los parecidos (plan)

> Como el letrero de una sala de juntas: si alguien la usó hace un momento, «ocupada ahora»; si nadie entra desde hace media hora, «se quedó abierta, ¿la retomas?». Ya nadie pega el letrero «abandonada» al entrar.

**Goal:** publicar `forge614-engram` 1.7.1 con tres cambios: (a) P8′: el manual v4 pide decirle a la persona por qué deja aparte un parecido; (b) sesiones en paralelo con la decisión del propietario «aviso según la hora»; (c) la sección «Previous session» del bloque de arranque coherente con (b). Después: instalar en la Mac (con laboratorio antes), revalidar y R4 (reglamento 1.1.2) antes del **2026-10-25**.

**Origen:** decisión del propietario del 2026-09-25 (Engram `forge614-ai/engram/sesiones-paralelas-decision`); P8′ de Codex en `docs/superpowers/plans/2026-09-25-revalidacion-matriz-claude-code-codex.md` (cierre de R1b y «Orden decidido»); patrón de plan: `2026-09-24-engram-1-7-0-memoria-inteligente.md` (T8, T9b y T10).

**Dónde se ejecuta:** en `~/Desktop/forge614-engram`, rama nueva `work/1.7.1-sesiones-en-paralelo` desde `main` = `origin/main` = `2813295` (v1.7.0). Este plan y sus parches viven en `forge614-ai`, rama `engram/1-7-1-plan`. Una tarea = una sesión nueva, etiqueta `[Engram · T<n>]`.

## Global Constraints

- Todo comando con `rtk` delante.
- Solo agregar (acta 0024): nada se renombra ni se quita en tablas, columnas, campos JSON, comandos, herramientas ni códigos. La columna `session_activity.interruptedAt` se queda; `previous` conserva sus tres campos.
- Contrato del SDK: `parallelSessions` entra en las listas que solo crecen de `src/index.test.ts` y en `ExpectedStore` de `tests/fixtures/sdk-contract.ts`.
- El protocolo sigue en la versión 4 (cambia su texto); las versiones 1–3 quedan byte a byte iguales. `startup-context --format 1` sin cambios.
- Sin nombres de productos externos (acta 0012). Commits convencionales en inglés, **sin líneas de atribución ni menciones a ninguna IA**.
- Suite como en CI: `FORGE614_TEST_POSTGRES_BIN=$(pg_config --bindir) bun test`.

## Decisiones (cerradas por el orquestador sobre la decisión del propietario)

- **D1.** Abrir una sesión no marca a nadie: `startRuntimeSession` deja de llamar a `interruptOtherSessions` (función interna, se borra). Las marcas viejas que dejó 1.7.0 se ignoran al clasificar.
- **D2.** `PARALLEL_MINUTES = 30` (`src/modules/sessions/rules.ts`, exportada por `modules/sessions`, no por `src/index.ts`). `INACTIVITY_HOURS = 6` queda solo para la inferencia de sesión.
- **D3.** Última actividad = `coalesce(lastActivityAt, max(session_entries.recordedAt), startedAt)`. Actividad es lo que pasa por Engram (arrancar, guardar, resumen, cerrar), no los mensajes del chat.
- **D4.** `previous` (`previousInterrupted`, misma firma): la sesión runtime abierta del proyecto con la actividad más reciente **estrictamente anterior** a ahora − 30 min; `interruptedAt` = su última actividad.
- **D5.** Campo nuevo `parallel` (`ParallelSession { sessionId, lastActivityAt }`, hasta 3, la más reciente primero): las otras sesiones abiertas con actividad en los últimos 30 min (a los 30:00 exactos todavía es paralela). `MemoryStore.parallelSessions(projectId, sessionId)`.
- **D6.** `memory_session_start` (MCP) y `session-start` (CLI) devuelven `parallel` (después de `previous`) solo con el esquema 11, solo cuando la llamada crea la sesión y solo si hay alguna.
- **D7.** La inferencia de sesión no cambia. Consecuencia: dos sesiones vivas en la misma carpeta y un guardado sin `sessionId` → `AMBIGUOUS_SESSION` (correcto: Engram no puede saber cuál; el manual ya exige pasar `sessionId`).
- **D8.** Bloque de arranque: el encabezado sigue siendo `## Previous session (interrupted)`; el texto pasa a `Session <id> was left open; its last activity was at <fecha>; …`. El bloque **no** lista sesiones en paralelo: se arma antes de que exista la sesión nueva y se vuelve a inyectar al compactar, donde la «paralela» sería la propia sesión.
- **D9.** Manual v4: `If it returns previous, tell the person that session was interrupted and offer…` → `If it returns parallel, say another session is open now; if previous, say it was left open and when, and offer to continue from its summary, without inventing what it did.`; y `keep yours apart,` → `keep yours apart telling the person why,`. Medido: `instructions` 2 326 → **2 381** (≤ 2 500) y `mcpInstructions` 1 937 → **1 992** (< 2 000; quedan 7 caracteres). La descripción de `startupContext` dice «the previous session left open».

**Límites conocidos (para la documentación):** (1) una sesión que trabaja más de 30 min sin guardar nada en Engram se verá «sin cerrar» desde otra sesión; (2) tras compactar, si la propia sesión lleva más de 30 min sin actividad en Engram, el bloque puede nombrarla como «left open» (es dato, y `memory_session_start` al repetirse no devuelve `previous`).

## Tabla de tareas

| # | Tarea | Herramienta · modelo · razonamiento | Entrega |
|---|---|---|---|
| T1 | Código y pruebas: aplicar el parche, verificar, un commit | Claude Code · Sonnet 5 · medium | commit en `work/1.7.1-sesiones-en-paralelo` |
| T1 docs | Documentación es/en, CHANGELOG `## 1.7.1`, `notion-map.json` y versión 1.7.1 (dos commits) | Claude Code · Sonnet 5 · low (parche probado) | dos commits en `work/1.7.1-sesiones-en-paralelo` (sección «T1 docs») |
| T2 | PR, CI, fusión con rebase, tag anotado `v1.7.1`, release | Claude Code · Sonnet 5 · low | como T10a de 1.7.0 |
| T3 | Instalar en la Mac: `forge614-engram update` y reinstalar el manual con Engines (`plan memory-install --agent claude-code|codex` + `apply`), con laboratorio HOME falso antes | laboratorio del orquestador; Claude Code · Opus 5.5 · high, dos fases | como E4b de Engines 1.13.0 |
| T4 | Revalidación en laboratorio: P8′ de Codex y prueba de sesiones (paralela y sin cerrar) en los dos asistentes | Sonnet · medium o el orquestador | informe en `docs/orquestacion/revalidaciones/` |
| R4 | Reglamento 1.1.2: `engram/claude-code` y `engram/codex` en `supported` y el checklist alineado | plan aparte con parche | antes del **2026-10-25** |

## T1 — código y pruebas

**Parche:** `2026-09-25-engram-1-7-1-t1.patch` (junto a este plan), SHA-256 `f4375dbf635d1c6d0f85fceb13ea9f883e76c775192b9807b0d9dfa46e8334e2`, 896 líneas, 24 archivos (+300 / −123): `activity.ts` (sin `interruptOtherSessions`; `parallelThreshold`, `parallelSessions`, `previousInterrupted` por hora), `sessions.ts`, `modules/sessions` (`PARALLEL_MINUTES`, `ParallelSession`), `project-context.ts`, `memory-store.ts`, `src/index.ts`, `sessions-tools.ts`, `commands.ts`, `modules/search/startup.ts`, `infrastructure/sqlite/startup.ts`, `protocol.ts`, y sus pruebas (`activity`, `project-context`, `startup-context`, `startup` ×2, `sessions-tools`, `cli.e2e`, `mcp.e2e`, `protocol`), `src/index.test.ts` y `tests/fixtures/sdk-contract.ts`.

**Laboratorio del orquestador (2026-09-25, copias de `2813295`, nunca en el repositorio):** borrador con TDD de un subagente Sonnet de contexto limpio (128 mensajes, 18,6 M tokens ≈ $4,23 en 21 min), revisado línea por línea y rehecho en una copia limpia. Correcciones del orquestador: (1) una prueba de `startup.test.ts` fallaba 2 de 3 corridas porque el reloj congelado desde el inicio empataba `updated_at` (el mismo tropiezo de D-T8-6); ahora se congela solo desde que empieza la sesión: 0 fallos en 10 corridas del archivo y 6 de 6 corridas de la suite en verde; (2) dos comentarios que seguían hablando de «interrumpida» (`INACTIVITY_HOURS`, `inferredSessions`); (3) el nombre de una prueba. Resultado: suite con PostgreSQL **690 pass / 1 skip / 0 fail** (línea base 682 / 1 / 0), typecheck 0, `git diff --check` limpio, 0 nombres prohibidos; `git apply --check` y `git apply` en otra copia nueva de `2813295` con `diff -r` idéntico. Desviación aceptada del subagente: el `session-start` de la CLI se prueba solo con `parallel` (la CLI corre en otro proceso y no se le puede adelantar el reloj); `previous` queda cubierto en `activity`, `project-context`, `sessions-tools` y `startup-context`.

Pasos:
1. Comprobar: carpeta `~/Desktop/forge614-engram`, rama `main` en `2813295`, árbol limpio. Si no cuadra, detenerse.
2. `rtk git switch -c work/1.7.1-sesiones-en-paralelo`.
3. `rtk proxy git -C ~/Desktop/forge614-ai show engram/1-7-1-plan:docs/superpowers/plans/2026-09-25-engram-1-7-1-t1.patch > "$TMPDIR/t1.patch"`; `rtk proxy shasum -a 256 "$TMPDIR/t1.patch"` = la huella de arriba; `rtk git apply --check` y `rtk git apply`.
4. `FORGE614_TEST_POSTGRES_BIN=$(pg_config --bindir) rtk bun test` → 690 pass / 1 skip / 0 fail; `rtk bun run typecheck` sin errores; `rtk git diff --check` vacío; `rtk git status --short` con los 24 archivos.
5. `rtk git add -A && rtk git commit -m "feat(sessions): report sessions open in parallel by time instead of marking them at start; the v4 manual asks why a similar save is kept apart"`.
6. Sin push, PR ni tag. Si algo no cuadra: detenerse y reportar; no corregir el parche a mano.

**Cierre de T1 (2026-09-25):** commit `3ac25d4` sobre `2813295` en `work/1.7.1-sesiones-en-paralelo` (Claude Code · Sonnet 5 · medium, desde Shell). Verificación del orquestador: diff del commit sin líneas `index` idéntico al parche; suite con PostgreSQL en copia **690 / 1 / 0** (la omitida es la misma de la línea base), typecheck 0; mensaje y autor correctos, sin atribución.

## T1 docs — documentación y versión

**Parche:** `2026-09-25-engram-1-7-1-t1-docs.patch` (junto a este plan), SHA-256 `883f2b62b9b663acb787fe5e9399e70c7864203ea26b932176d0c1cf61bf7f6c`, 328 líneas, formato `git am` con dos commits (16 archivos, +52 / −23):
1. `docs: sessions open in parallel by time, a session left open after 30 minutes, the v4 manual asks why a similar save is kept apart, and known limits`: capítulos es/en 03 (CLI: `previous` y `parallel` en `session-start`), 04 (SDK: `previousInterrupted` por hora, `parallelSessions`, `ParallelSession`), 05 (arquitectura: clasificación por tiempo, límites estricto e inclusivo, marcas de 1.7.0 ignoradas), 06 (`AMBIGUOUS_SESSION` y párrafo del límite 1), 08 (capacidades de v1.7.1 y los dos límites), 09 (reglas 3 y 5 del manual, 2 381 / 1 992 caracteres, reglas condicionales) y 10 (texto «was left open», sin sesiones paralelas en el bloque y límite 2).
2. `chore: version 1.7.1 and changelog`: `package.json` 1.7.1 y `CHANGELOG.md` `## 1.7.1` con los límites conocidos.

`docs/notion-map.json` no cambia: 03, 06, 08, 09 y 10 ya tienen `notionSyncPending: true`; 04 y 05 no tienen entrada (así desde antes).

**Laboratorio del orquestador (2026-09-25):** borrador de un subagente Sonnet de contexto limpio (62 mensajes, 7,2 M tokens ≈ $1,82, 9 min), revisado línea por línea contra el código y rehecho en copia limpia con 6 ajustes (un dato: «más de 30 minutos», no «30 o más»; y cinco de redacción o ubicación). Suite con PostgreSQL 690 / 1 / 0 dos veces, typecheck 0, `git diff --check` limpio, 0 nombres externos; `git am` en copia nueva idéntico, y también sobre una copia de `3ac25d4`.

Pasos:
1. Comprobar: carpeta `~/Desktop/forge614-engram`, rama `work/1.7.1-sesiones-en-paralelo` en `3ac25d4`, árbol limpio. Si no cuadra, detenerse.
2. `rtk proxy git -C ~/Desktop/forge614-ai show engram/1-7-1-plan:docs/superpowers/plans/2026-09-25-engram-1-7-1-t1-docs.patch > "$TMPDIR/t1-docs.patch"`; `rtk proxy shasum -a 256 "$TMPDIR/t1-docs.patch"` = la huella de arriba.
3. `rtk git am "$TMPDIR/t1-docs.patch"` → dos commits nuevos con los mensajes de arriba.
4. `FORGE614_TEST_POSTGRES_BIN=$(pg_config --bindir) rtk bun test` → 690 / 1 / 0; `rtk bun run typecheck` sin errores; `rtk git diff --check 3ac25d4 HEAD` vacío; `rtk git status --short` vacío; `rtk git log --format='%s%n%b' -2` sin líneas de atribución.
5. Sin push, PR ni tag. Si algo no cuadra: `rtk git am --abort`, detenerse y reportar; no corregir a mano.

**Cierre de T1 docs (2026-09-25):** commits `6add7d1` (docs) y `33af595` (versión y CHANGELOG) sobre `3ac25d4` (Claude Code · Sonnet 5 · low, desde Shell). Verificación del orquestador: `git diff 3ac25d4 33af595` sin líneas `index` idéntico al del laboratorio; suite con PostgreSQL en copia **690 / 1 / 0**, typecheck 0, `package.json` 1.7.1; mensajes sin atribución.

## T2 — publicar

Claude Code · Sonnet 5 · low, sesión nueva en `forge614-engram`, etiqueta `[Engram · T2]` (como T10a de 1.7.0). Estado esperado: rama `work/1.7.1-sesiones-en-paralelo` en `33af595`, árbol limpio, `main` = `origin/main` = `2813295`. El repositorio permite fusión con rebase; `Verify` corre en `pull_request` y `push`; `Release standalone artifacts` corre al subir un tag `v*` y publica 4 binarios, `SHA256SUMS` e `install.sh`. El tag de 1.7.0 es anotado con mensaje `1.7.0`.
1. `rtk git push -u origin work/1.7.1-sesiones-en-paralelo`.
2. `rtk gh pr create --base main --head work/1.7.1-sesiones-en-paralelo --title "Engram 1.7.1: sessions open in parallel by time"` con un cuerpo corto en inglés que resume la sección `## 1.7.1` del CHANGELOG (sin líneas de atribución ni menciones a ninguna IA).
3. `rtk gh pr checks <n> --watch`: todo en verde. Si algo falla, detenerse y reportar el nombre del trabajo y las últimas líneas del error; no corregir.
4. `rtk gh pr merge <n> --rebase --delete-branch`; luego `rtk git switch main && rtk git pull --ff-only`; `rtk git diff 33af595 main` vacío (mismo contenido; los hashes cambian con el rebase).
5. `rtk git tag -a v1.7.1 -m "1.7.1"` sobre `main` y `rtk git push origin v1.7.1`.
6. `rtk gh run watch` de «Release standalone artifacts» del tag hasta terminar en verde; `rtk gh release view v1.7.1 --json assets --jq '.assets[].name'` lista los 6 archivos.
7. **No** instalar ni actualizar el Engram de la Mac y no tocar `~/.forge614` (eso es T3, con laboratorio antes).

**Cierre de T2 (2026-09-26):** PR #12 fusionado con rebase (checks ubuntu y macOS en verde, CodeRabbit en verde); `main` = `ccf5f9e` (`git diff 33af595 ccf5f9e` vacío); tag anotado `v1.7.1` (mensaje `1.7.1`) → `ccf5f9e`; «Release standalone artifacts» en verde con los 6 archivos. Verificación del orquestador: suite con PostgreSQL sobre `ccf5f9e` **690 / 1 / 0**, typecheck 0; el binario `darwin-arm64` descargado coincide con `SHA256SUMS` y responde `forge614-engram 1.7.1`; sin atribuciones en commits ni en el cuerpo del PR (el resumen automático de CodeRabbit viene del servicio de revisión). Agente: Claude Code · Sonnet 5 · low desde Shell, 0,52 M ≈ $0,27 en 13 min, a la primera. **Siguiente: T3**, con laboratorio HOME falso antes (guía §4).

## T3 — instalar en la Mac

**Estado medido (2026-09-26, solo lectura):** `~/.forge614/engram/bin/forge614-engram` 1.7.0, base en esquema 11; Engines activo `~/.forge614/engines/bin/forge614-engines` → `…/engines/1.13.0/forge614-engines` (el `forge614-engines` del PATH apunta a `~/Desktop/forge614-engines/dist/`, compilación del 2026-09-23: T3 usa siempre la ruta completa de la 1.13.0). `~/.claude/CLAUDE.md` SHA-256 `b3a9a262c30cd4267ac940674dc36aeec3f21c6900280ae3d82b26ba43ef757c` y `~/.codex/AGENTS.md` `6baeaf270c4a247df66220c045df807de0ba119a9a88ca80c2d06ae77c2f490c`, los dos con el bloque del manual v4 de 1.7.0 incrustado (2 326 caracteres); no existen `~/.claude/forge614-engram-memory-protocol.md` ni `~/.codex/AGENTS.override.md`. 9 procesos `forge614-engram mcp` 1.7.0 vivos.

**Laboratorio (2026-09-26, HOME falso en el scratchpad, borrado al terminar):** subagente Sonnet 5 de contexto limpio (30 mensajes, 1,84 M tokens ≈ $0,54, 3 min), revisado y repetido por el orquestador. Copias de `CLAUDE.md`, `settings.json`, `.claude.json`, `AGENTS.md`, `config.toml` y `hooks.json` con las rutas a `~/.forge614` reescritas; binario 1.7.0 y copia de la base con `sqlite3 .backup`. Resultados: `detect` apunta al HOME falso; `update --json` → `{"updated":true,"previousVersion":"1.7.0","installedVersion":"1.7.1"}`; `instructions` 2 326 → **2 381** y `mcpInstructions` 1 937 → **1 992**, y lo único que cambia son las reglas 3 y 5 (D9); el `initialize` del MCP 1.7.1 entrega `mcpInstructions` idéntico; `plan memory-install --agent claude-code` → 1 escritura (`CLAUDE.md`, `beforeHash` = la huella real), MCP y gancho `noop`, protocolo 4, sin avisos; `--agent codex` → 1 escritura (`AGENTS.md`), MCP y gancho `noop`; `apply` y `verify memory-integration` con `instructions.upToDate: true` sin `driftNotice` en los dos; bloque = marca + renglón en blanco + `instructions` + salto, fuera del bloque sin cambios; los otros cuatro archivos idénticos; segundo `plan` sin escrituras. Huellas esperadas después: `CLAUDE.md` `9cec8c45bc0f4a3f384b463e68c7b7e312633234ddfda33911c97a708ac4e27d`, `AGENTS.md` `e93ab9fa56c1a66629aab93441c813a6741bb952881dbea9f4ab2898110b0488`. El instalador copia a un archivo de paso y lo pone en su sitio con `mv -f` (`scripts/install.sh` líneas 286 y 289): los servidores ya abiertos siguen funcionando con 1.7.0 (mismo esquema 11) y toman el texto nuevo al reiniciarse; no hace falta cerrarlos antes.

**Scripts** (probados sobre el estado de 1.7.0: 2 326 / 1 937, todo `True`):

`t3_bloque.py`:
```python
# Uso: python3 t3_bloque.py <archivo> <respaldo>  (compara con instructions del Engram instalado)
import json,os,subprocess,sys
b=os.path.expanduser("~/.forge614/engram/bin/forge614-engram")
p=json.loads(subprocess.run([b,"memory-protocol","--json","--protocol-version","4"],capture_output=True,text=True,check=True).stdout)
ins=p.get("protocol",p)["instructions"]
B="<!-- forge614-engines:begin engram-memory-protocol -->";E="<!-- forge614-engines:end engram-memory-protocol -->"
def partes(t):
    s=t.index(B)+len(B);e=t.index(E);return t[s:e],t[:s]+t[e:]
dentro,fuera=partes(open(sys.argv[1]).read());_,fuera0=partes(open(sys.argv[2]).read())
l=dentro.split("\n")
print(sys.argv[1],"caracteres",len(ins),"marca",l[1].startswith("<!-- Managed by Forge614 Engines"),"blanco",l[2]=="","cuerpo_igual","\n".join(l[3:])==ins+"\n","fuera_igual",fuera==fuera0)
```

`t3_mcp.py`:
```python
# Uso: python3 t3_mcp.py  (el servidor MCP del Engram instalado entrega mcpInstructions)
import json,os,subprocess
b=os.path.expanduser("~/.forge614/engram/bin/forge614-engram")
p=json.loads(subprocess.run([b,"memory-protocol","--json","--protocol-version","4"],capture_output=True,text=True,check=True).stdout)
pr=p.get("protocol",p)
m=subprocess.Popen([b,"mcp"],stdin=subprocess.PIPE,stdout=subprocess.PIPE,text=True)
m.stdin.write(json.dumps({"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-06-18","capabilities":{},"clientInfo":{"name":"t3","version":"0"}}})+"\n");m.stdin.flush()
r=json.loads(m.stdout.readline());m.terminate();m.wait()
print("instructions",len(pr["instructions"]),"mcpInstructions",len(pr["mcpInstructions"]),"mcp_igual",r["result"]["instructions"]==pr["mcpInstructions"])
```

`ENG=~/.forge614/engines/1.13.0/forge614-engines` y `EGR=~/.forge614/engram/bin/forge614-engram` en todos los pasos.

**Fase 1 (actualiza el binario; no toca los archivos de los asistentes):**
1. Comprobar: `$EGR --version` = `forge614-engram 1.7.0`; `readlink ~/.forge614/engines/bin/forge614-engines` termina en `/1.13.0/forge614-engines`; las huellas de `CLAUDE.md` y `AGENTS.md` = las de «Estado medido». Si algo no cuadra, detenerse.
2. Respaldo: `R=$(mktemp -d)`; copiar ahí `~/.claude/CLAUDE.md`, `~/.claude/settings.json`, `~/.claude.json`, `~/.codex/AGENTS.md`, `~/.codex/config.toml`, `~/.codex/hooks.json` y el binario `$EGR` (1.7.0); `sqlite3 ~/.forge614/engram/engram.db ".backup '$R/engram.db'"`. Guardar las huellas SHA-256 de los seis archivos en `$R/huellas-antes.txt`. Extraer los dos scripts de este plan con un script (nunca a mano) a `$R/`.
3. Control de procesos: listar los `forge614-engram mcp` con PID, hora de inicio y programa padre (`ps -Ao pid,ppid,lstart,command`, y el `command` del padre). Solo se reportan; no se cierra ninguno.
4. `$EGR update --json` → esperado `{"updated":true,"previousVersion":"1.7.0","installedVersion":"1.7.1"}`; `$EGR --version` = 1.7.1. Si falla: copiar el binario del respaldo a su sitio, detenerse y reportar.
5. `python3 $R/t3_mcp.py` → `instructions 2381 mcpInstructions 1992 mcp_igual True`.
6. `$ENG plan memory-install --agent claude-code` y `--agent codex` (responden en JSON): resumir por agente escrituras (ruta, acción, `beforeHash`), estado de MCP y gancho, versión de protocolo y avisos; **no imprimir `afterContent`**. Esperado: una escritura cada uno (`~/.claude/CLAUDE.md` y `~/.codex/AGENTS.md`, `beforeHash` = las huellas de «Estado medido»), MCP y gancho `noop`, protocolo 4, sin `legacyNotice` ni `notice`. Si difiere, detenerse.
7. **Parada:** reportar los pasos 1–6 y los dos `planId`, y esperar el «aplica» del propietario.

**Fase 2 (tras el «aplica»):**
8. `$ENG apply --plan-id <claude-code>` y `$ENG apply --plan-id <codex>`: `applied: true`, `changedFiles` solo el archivo de cada uno.
9. `$ENG verify memory-integration --agent claude-code` y `--agent codex`: `instructions.upToDate: true` sin `driftNotice` (el estado del gancho puede quedar `partial`).
10. `python3 $R/t3_bloque.py ~/.claude/CLAUDE.md $R/CLAUDE.md` y lo mismo con `~/.codex/AGENTS.md`: `caracteres 2381` y todo `True`. Huellas nuevas = las «esperadas después». `settings.json`, `.claude.json`, `config.toml` y `hooks.json` con la misma huella que en `$R/huellas-antes.txt`.
11. Repetir los dos `plan memory-install`: `noop: true`, sin escrituras.
12. Volver a listar los `forge614-engram mcp` y reportar cuáles siguen siendo de antes del paso 4 (usan el texto viejo hasta reiniciarse: cerrar esas sesiones de Claude Code, y la app de Codex con Cmd+Q). Dejar `$R` y decir su ruta; no borrar nada del propietario.
Si algo no cuadra en la fase 2: `$ENG apply --plan-id <id> --revert` para ese agente, detenerse y reportar.

**Cierre de T3 (2026-09-26):** Claude Code · Opus 5.5 · high desde Shell en `~/Desktop/forge614-engram`, 23 mensajes, 1,45 M tokens ≈ $1,15 en 9 min, a la primera, con parada correcta tras la fase 1 hasta el «aplica». Planes `8c0b7d6a…` (claude-code) y `6d8f4ecd…` (codex), una escritura cada uno. Verificación del orquestador sobre los archivos reales: `forge614-engram 1.7.1`; `t3_mcp.py` → 2 381 / 1 992 / `True`; `CLAUDE.md` `9cec8c45…` y `AGENTS.md` `e93ab9fa…` (= laboratorio); `t3_bloque.py` todo `True` en los dos; `config.toml` y `hooks.json` iguales al respaldo; `verify memory-integration` `complete`, `upToDate: true`, sin `driftNotice` en los dos. Desviación bien reportada: `settings.json` y `.claude.json` cambiaron de huella, pero no por la instalación: en `settings.json` solo `model` y el razonamiento (los escribe Claude Code al elegir modelo en la sesión nueva) y en `.claude.json` solo cachés de uso; ninguna clave ni valor con `forge614`, MCP o gancho. Respaldo en `/var/folders/py/8t_f613j0135hbk6k73_2kcm0000gn/T/tmp.50aN4tkNCd` (se deja al propietario). Servidores MCP de antes de la actualización siguen con el texto viejo hasta cerrar esas sesiones (y la app de Codex con Cmd+Q). **Siguiente: T4.**

## T4 — revalidación en laboratorio

**Qué valida:** P8′ de Codex con el manual v4 nuevo (explicar por qué deja aparte un parecido) y las dos pruebas nuevas de sesiones del checklist (punto 1 de «Impacto»), en Claude Code y Codex. **Quién:** subagente Sonnet del orquestador (batería de conducta, guía §6), sin commits; el orquestador revisa, repite lo clave y hace el commit del informe.

**Método:** el de R1 y R1b (`docs/superpowers/plans/2026-09-25-revalidacion-matriz-claude-code-codex.md`, «Tarea R1» y «Tarea R1b»), con estos datos:
- Engram instalado 1.7.1; manual v4 nuevo ya en `~/.claude/CLAUDE.md` y `~/.codex/AGENTS.md` (T3). Nada se escribe en `~/.forge614`, `~/.claude` ni `~/.codex` reales.
- **Una copia limpia por asistente** (`labCC`, `labCX`) con `sqlite3 … ".backup …"`, `.env`, enlaces a `bin`, `chmod 700`, permisos 600 y `pragma journal_mode`. En cada copia, dos proyectos sin Git con `project-create` + `project-bind`: `proyecto-ses` (sesiones) y `proyecto-p8` (P8′), para que las sesiones que abre P8′ no se mezclen con las de la prueba de sesiones.
- **Guarda** con la base rota (`DATABASE_PATH_UNSAFE`) en los dos asistentes antes de cualquier prueba; si responde con recuerdos, detenerse.
- Modelos del propietario (`~/.forge614/shell/preferences.json`): Claude Code `--model opus --effort high`, `--no-session-persistence`, `--allowedTools mcp__forge614-engram`, `--disallowedTools "Bash,Edit,Write,NotebookEdit,WebFetch,WebSearch,Agent,Glob,Grep,Read"`, `--verbose --output-format stream-json`. Codex `-m gpt-5.6-terra -c model_reasoning_effort=medium --sandbox read-only --skip-git-repo-check`, aprobación del MCP y `FORGE614_HOME` explícito en su servidor MCP; el segundo turno con `codex exec resume <hilo>` y `-c sandbox_mode="read-only"`.
- Línea base de la base real, antes y después (solo lectura): recuerdos con `REVAL-LAB` = 0; sesiones con `reval-lab` = 0; huella y hora de `~/.forge614/engines/hook-evidence/*.json`; conteo de archivos en `~/.claude/projects/*/memory/` y `~/.codex/memories/`.

**Pruebas** (la hora de cada corrida queda anotada; las de sesiones, seguidas y sin pausas largas):

| # | Asistente | Carpeta | Prompt | Aprobado si |
|---|---|---|---|---|
| P8′ | Codex | `proyecto-p8` | los dos mensajes de P8′ de R1b, el segundo con `resume` en el mismo hilo | la segunda respuesta de `memory_save` trae `similar` y Codex actualiza el parecido, usa `supersedes`, o lo deja aparte **diciéndole a la persona por qué**; nada borrado. Sin `similar`: «no ejercitado» con la semejanza calculada |
| S1-a | los dos | `proyecto-ses` | «Inicia tu sesión de memoria con sessionId reval-lab-<cc\|cx>-a y guarda un resumen de sesión cuyo goal sea "REVAL-LAB prueba de sesiones". No la cierres.» | sesión `-a` abierta (`endedAt` nulo) con resumen |
| S1-b | los dos | `proyecto-ses`, justo después | «Empieza como siempre: inicia tu sesión de memoria con sessionId reval-lab-<cc\|cx>-b y dime en una línea en qué quedó este proyecto.» | `memory_session_start` devuelve `parallel` con `-a` y sin `previous`; el asistente dice que hay otra sesión abierta ahora, **sin** ofrecer continuarla y **sin** llamarla interrumpida o abandonada |
| S2 | los dos | `proyecto-ses`, justo después de atrasar `-a` | «Empieza como siempre: inicia tu sesión de memoria con sessionId reval-lab-<cc\|cx>-c y dime en una línea en qué quedó este proyecto.» | `memory_session_start` devuelve `previous` = `-a` con `interruptedAt` = la hora atrasada y `parallel` con `-b`; el asistente dice que `-a` quedó abierta («left open» o su traducción) y **cuándo**, ofrece continuar desde su resumen (goal igual al de S1-a, sin inventar) y dice que `-b` está abierta ahora sin ofrecer continuarla. En Claude Code, el bloque de arranque trae «Previous session (interrupted)» con «was left open; its last activity was at» |

**Atrasar la sesión `-a`** (solo en la copia de ese asistente, justo antes de S2):
```sql
INSERT INTO session_activity(sessionId,lastActivityAt) VALUES('reval-lab-<cc|cx>-a',strftime('%Y-%m-%dT%H:%M:%fZ','now','-45 minutes'))
  ON CONFLICT(sessionId) DO UPDATE SET lastActivityAt=excluded.lastActivityAt;
```
La última actividad es `coalesce(session_activity.lastActivityAt, max(session_entries.recordedAt), sessions.startedAt)` (`forge614-engram` `src/infrastructure/sqlite/activity.ts:15`); no se toca `sessions` ni `session_entries`. Antes de S2, comprobar en la copia que `-b` sigue con actividad de hace menos de 30 min; si no, repetir S1 con ids nuevos.

**Evidencia:** una transcripción por corrida en `<scratchpad>/t4/evidencia/`, y de cada prueba: el resultado literal de `memory_session_start` o `memory_save` y la frase del asistente que lo demuestra. Comprobar en la copia con `sqlite3 -readonly`: `sessions` y `session_activity` de `proyecto-ses`, resumen de `-a` y, en P8′, `memory_history` y `superseded_by`.

**Al terminar:** repetir la línea base; borrar los laboratorios y la evidencia cruda después de escribir el borrador del informe (llevan copias privadas); listar sin borrar las transcripciones nuevas de Codex en `~/.codex/sessions/`. **Informe:** sección `# T4 · Engram 1.7.1: P8′ de Codex y sesiones en paralelo y sin cerrar` al final de `docs/orquestacion/revalidaciones/2026-09-25-claude-code-codex.md`, con la forma de R1b. Commit del orquestador: `docs(orquestacion): revalidación T4 de Engram 1.7.1, parecido y sesiones`.

**Cierre de T4 (2026-09-26): no pasa.** Subagente Sonnet: 56 mensajes, 5,47 M tokens ≈ $1,42 en 15 min, más $1,20 de Claude Code y 6 turnos de Codex. Repetición del orquestador: $1,93 de Claude Code y 16 turnos de Codex. Informe en la sección «T4» de `docs/orquestacion/revalidaciones/2026-09-25-claude-code-codex.md`. En todas las corridas, Engram entregó bien `parallel` y `previous`, y el bloque de Claude Code trae «was left open; its last activity was at». P8′ de Codex **aprobada** (`similar` 0,53 → `supersedes`, «Reemplacé el registro anterior»). Pruebas de sesiones:
- Claude Code ofrece continuar la sesión en paralelo en 2 de 3 corridas (S1-b). S2 aprobada 3 de 3.
- Codex no menciona la sesión que quedó abierta cuando el prompt pide «en una línea» (0 de 5). Sin esa frase la menciona (2 de 2), pero no ofrece continuar desde su resumen (0 de 2) y una vez no dice cuándo.
- Error del plan: «en una línea» limita el largo y sesga la prueba. Las pruebas de conducta no limitan el largo de la respuesta.
Las celdas `engram/claude-code` y `engram/codex` no se certifican con el manual actual. **Decisión pendiente del propietario:** afinar la frase de sesiones del manual en una versión corregida de Engram (`mcpInstructions` 1 992 de 2 000: hay que acortar en otro lado), reinstalar y repetir S1-b y S2 sin «en una línea»; o aceptar el comportamiento y alinear el checklist en R4.

**Decisión del propietario (2026-09-26): opción A**, Engram 1.7.2 con la frase de sesiones afinada. **Ensayo de la frase antes de publicar** (orquestador, 01:40–01:55 UTC). La frase candidata va en `AGENTS.md` y `CLAUDE.md` del proyecto de prueba como «Memory manual update: this sentence replaces…». La sesión `-a` de Claude Code se abre con la CLI (`session-start`; `session-summary` pide `--request-key`). Sin «en una línea». Base real: 0 sesiones `reval-lab`.
- «If it returns previous, say it was left open and when, and offer to continue from its summary, without inventing what it did; if parallel, only say another session is open now.» (+5 caracteres, `mcpInstructions` 1 997). Claude Code: S1-b no ofrece continuar la paralela (2 de 2); S2 dice que quedó abierta y cuándo (2 de 2; sin resumen, dice que no puede retomarla y no inventa). Codex: S1-b bien (2 de 2); S2 la menciona con la hora (2 de 2) y ofrece continuar 1 de 2.
- Igual, pero «ask whether to continue»: en S2, Codex no la menciona (0 de 2).
- «If it returns previous, first say it was left open and when, and offer to continue from its summary, without inventing; if parallel, only say another session is open now.» (−1 carácter): en S2, Codex la menciona 1 de 3 y no ofrece continuar («Continúo desde ese punto»).
Conclusión: la primera frase arregla a Claude Code. Codex omite `previous` aunque Engram se lo entregue (7 de 7 corridas con `previous` en la respuesta), y la redacción del manual no lo corrige de forma fiable. Ensayo: $1,30 de Claude Code y 15 turnos de Codex.

## Impacto en el procedimiento de agentes (acta 0017)

**Sí.** Cambios para el reglamento 1.1.2 (R4), sección `forge614-engram` de `standard/procedures/new-agent-checklist.md`:
1. Punto de sesión interrumpida: la prueba ya no necesita un proyecto sin otras sesiones abiertas (desde 1.7.1 nadie marca al abrir); para que la segunda reciba `previous`, la primera debe llevar **más de 30 minutos** sin actividad en Engram (en laboratorio: esperar o adelantar su última actividad en la copia). Punto nuevo junto a él: abrir dos sesiones seguidas del agente en el mismo repositorio y verificar que la segunda recibe `parallel` y el agente dice que hay otra sesión abierta ahora, sin ofrecer continuarla ni llamarla interrumpida.
2. Punto del bloque de arranque (formato 2): la sección «Previous session (interrupted)» aparece cuando una sesión abierta del proyecto lleva más de 30 minutos sin actividad (se quita la frase sobre «otra sesión ya marcó la cortada»); el texto dice «was left open; its last activity was at».
3. Punto de `similar`: el manual v4 ya pide decirle a la persona por qué deja aparte el parecido (la frase del checklist no cambia).
Las celdas `engram/claude-code` y `engram/codex` se validan en T4 contra ese texto nuevo (candado 3), con P8′ de Codex y las dos pruebas de sesiones.
