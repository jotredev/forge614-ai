# Reglamento 1.1.0 — la memoria inteligente de Engram en el procedimiento de agentes (plan de implementación)

**Goal:** publicar el reglamento (`standard/VERSION`) 1.1.0 con el acta 0027, los puntos nuevos y las correcciones de la sección `forge614-engram` del checklist de agentes, la fila del refuerzo del contrato del ecosistema y la matriz de soporte en revalidación para Engram.

**Origen:** los puntos quedaron redactados, tarea por tarea, en `docs/superpowers/plans/2026-09-24-engram-1-7-0-memoria-inteligente.md`, sección «Impacto en el procedimiento de agentes (acta 0017), tarea por tarea». Engram 1.7.0 está publicado y activo en la base del propietario desde el 2026-09-25.

**Dónde se ejecuta:** `~/Desktop/forge614-ai`, rama nueva `standard/reglamento-1.1.0` creada desde `engram/memoria-inteligente-spec` (commit que agrega este plan). Esa rama lleva 55 commits locales sin push por delante de `main` (`6da9f66`), 0 por detrás: el spec y el plan de Engram 1.7.0, sus parches y la guía del orquestador. Un solo PR lleva todo (todo es documentación). Una tarea = una sesión nueva, etiqueta `[Reglamento · R<n>]`.

## Global Constraints

- Todo comando con `rtk` delante.
- Commits convencionales en español, **sin líneas de atribución ni menciones a ninguna IA**.
- Solo agregar (acta 0024): ninguna regla, punto, campo ni celda se quita; lo que cambia de texto se corrige, no se borra.
- Sin productos prohibidos (`standard/forbidden-mentions.json`); documentación es/en con el mismo contenido (el checklist de agentes solo existe en español).
- `CHANGELOG.md` no se edita a mano (lo genera `bun release` desde los commits).
- Archivos generados, nunca a mano: `docs/decisions/INDEX.json` (`bun run decisions:index`), `forge614.node.json.standard.sha256` (`bun run standard:pack -- --update-pointer`), huellas de `docs/notion-map.json` (`bun run notion-map:build`). **Orden obligatorio:** `decisions:index` antes de `standard:pack` (el paquete valida las actas y falla con `STANDARD_INVALID` si el índice no incluye la 0027).

## Tabla de tareas

| # | Tarea | Herramienta · modelo · razonamiento | Variable |
|---|---|---|---|
| R1 | Aplicar el parche, verificar y dos commits (sin push) | Claude Code · Sonnet 5 · medium | parche completo con `git apply` (regla firme, guía §6) |
| R2 | Push, PR, CI, fusión, tag `standard-v1.1.0` y release | Claude Code · Sonnet 5 · low | publicación (se detalla tras aprobar R1) |

## Qué cambia (contenido del parche)

Parche `2026-09-25-reglamento-1-1-0.patch` (junto a este plan), SHA-256 `7400ad48ef8b4ff7da4c216994f5d8c2ded4f0ec8ae30560bf1b86b49e7a268f`, 370 líneas, 15 archivos (159 inserciones, 23 borrados):

1. **Acta 0027** `docs/decisions/0027-memoria-inteligente-de-engram-en-el-procedimiento.md` (formato de `TEMPLATE.md`) y su entrada en `docs/decisions/INDEX.json`.
2. **Checklist** `standard/procedures/new-agent-checklist.md`, sección `forge614-engram`:
   - correcciones del hallazgo de T1: Decisión con las versiones 1, 3 y 4 del protocolo; versión reconocida «1 a 4»; siete herramientas MCP; cierre no obligatorio con la versión 4; alcance `ecosystem` en el primer punto de memorias durables;
   - puntos nuevos: T2 (`SECRET_REJECTED`), T4 (`ECOSYSTEM_*`) y P-T3 (parecidos) en «Memorias durables y seguridad»; P-T5 (sesión interrumpida), P-T7a (manual completo), P-T7b (resumen vivo) y T8 (base nueva en esquema 11) en «Compatibilidad con el protocolo público»; P-T6 (bloque de arranque, lado del host) tras el punto opcional de `startup-context`. Las etiquetas P-T* no aparecen en el checklist, solo en este plan y en el acta.
3. **Contrato del ecosistema** es/en, tabla de inicialización de memoria: «Refuerzo por memorias repetidas | Opcional» pasa a «Incluido en una base nueva (Engram ≥ 1.7.0, `init`); opcional hasta `intelligence-enable` en una base existente» (y su par en inglés).
4. **Versión 1.0.2 → 1.1.0** (todas las apariciones, con `g`): `standard/VERSION`, títulos de `STANDARD.md`/`.en.md`, documentos 00 y 01 es/en (incluido `standard-1.1.0.tar.gz`), `forge614.node.json` con la huella nueva y `docs/notion-map.json` con las huellas nuevas de 00 y 01. No cambia la cita histórica de 1.0.2 en el plan de Engram (línea ~682).
5. **Matriz de soporte:** las celdas `engram/claude-code` y `engram/codex` pasan de `not-applicable` a `revalidate` (`revalidateSince` 2026-09-25, `reason`, `deadline` 2026-10-25); el resto del archivo no cambia de formato. `engram/cursor` sigue en `not-applicable` (Cursor no tiene integración de memoria: no se ejecuta sin interfaz). Nota: el verificador no detecta solo que cambió una sección del checklist; el candado 3 de `STANDARD.md` §12 lo aplica quien cambia el reglamento, y el verificador falla si una celda pasa más de 30 días en `revalidate`.

**Laboratorio del orquestador (2026-09-25):** copia de `0a32b6a`; línea base `verify` 0, suite 322 pass / 2 skip / 0 fail, typecheck 0. Con el parche: `verify` 0 (`"standard":"1.1.0"`), `standard:pack -- --check` 0, suite **322 pass / 2 skip / 0 fail** (las 2 omitidas son de PowerShell, que no está instalado en la Mac; CI las corre en Windows), typecheck 0, `git diff --check` limpio; `git apply --check` y `git apply` sobre una copia nueva, luego `verify` 0 y `standard:pack -- --check` 0. Errores del borrador corregidos en el laboratorio: la matriz se reescribía entera con otra sangría; `sed` sin `g` dejaba `standard-1.0.2.tar.gz`; `standard:pack` antes de `decisions:index` fallaba; el texto del acta no estaba en el borrador.

## R1 — aplicar y verificar

Estado esperado: rama `engram/memoria-inteligente-spec` en el commit que agrega este plan y el parche, árbol limpio.

1. `rtk git switch -c standard/reglamento-1.1.0`.
2. `rtk proxy shasum -a 256 docs/superpowers/plans/2026-09-25-reglamento-1-1-0.patch` = el SHA-256 de arriba; `rtk git apply --check` y `rtk git apply` del parche.
3. Verificación (código de salida de cada una por separado): `rtk bun run verify` 0 con `"standard":"1.1.0"`; `rtk bun run standard:pack -- --check` 0; `rtk bun test` 322 pass / 2 skip / 0 fail; `rtk bun run typecheck` 0; `rtk git diff --check` vacío; `rtk git status --short` muestra exactamente los 15 archivos del parche.
4. Commit 1: `rtk git add docs/decisions/0027-memoria-inteligente-de-engram-en-el-procedimiento.md docs/decisions/INDEX.json && rtk git commit -m "docs(decisions): acta 0027, la memoria inteligente de Engram en el procedimiento de agentes"`.
5. Commit 2: `rtk git add -A && rtk git commit -m "feat(standard): reglamento 1.1.0 con la memoria inteligente de Engram en el checklist de agentes, el contrato y la matriz"`.
6. Sin push, sin PR y sin tag (R2).
7. Si algo no cuadra: detenerse y reportar; no corregir el parche a mano.

**Resultado R1 (2026-09-25):** sesión `0050d9d8` (Sonnet 5 medium). Ronda 1 detenida (parada correcta): el parche se aplicó bien, pero `verify`, `standard:pack -- --check` y 5 pruebas de `release-standard.test.ts` fallaron con `STANDARD_PACK_DRIFT` porque `dist/` (ignorado por Git, `.gitignore:7`) conservaba el paquete de 1.0.2; el laboratorio partió de `git archive`, sin `dist/` (**error del plan**). Ronda 2: `bun run standard:pack` regeneró `dist/` sin cambiar ningún archivo versionado; commits `9b3d7fd` (acta) y `7e2692a` (reglamento). Verificado por el orquestador: el árbol de `7e2692a` es idéntico a `bc6334c` + parche (`diff -r` vacío); en una copia, `verify` 0 con `"standard":"1.1.0"`, `standard:pack -- --check` 0, suite 322 / 2 / 0, typecheck 0; sin atribuciones. Agente: 0,61 M tokens ≈ $0,31 en 2 rondas. **R1 cerrada.**

## R2 — publicar

Claude Code · Sonnet 5 · low, sesión nueva en `forge614-ai`, etiqueta `[Reglamento · R2]`. Estado esperado: rama `standard/reglamento-1.1.0` en el commit que agrega esta sección, árbol limpio; `origin/main` = `6da9f66`. Referencia medida: 1.0.2 fue el PR #10 (checks `verify` y `parity` en ubuntu-24.04, macos-15 y windows-2025), fusionado con rebase, tag **ligero** `standard-v1.0.2` sobre el commit de `main`, y el workflow `standard-release` (se dispara con tags `standard-v*`: `standard:check` y `standard:release`) publicó 3 archivos: `standard-1.0.2.tar.gz`, `SHA256SUMS` y `pack-manifest.json`. `main` no tiene protección de rama.
1. `rtk git push -u origin standard/reglamento-1.1.0`.
2. `rtk gh pr create --base main --head standard/reglamento-1.1.0 --title "feat(standard): reglamento 1.1.0 con la memoria inteligente de Engram"` con un cuerpo corto en español: acta 0027, checklist de Engram (7 puntos nuevos, T8 y 5 correcciones), fila del refuerzo del contrato, versión 1.1.0, matriz con `engram/claude-code` y `engram/codex` en revalidación hasta el 2026-10-25, y que el PR también lleva el spec, el plan y los parches de Engram 1.7.0 y la guía del orquestador (solo documentación). Sin líneas de atribución ni menciones a ninguna IA.
3. `rtk gh pr checks <n> --watch`: `verify` y los tres `parity` en verde. Si algo falla, detenerse y reportar el nombre del trabajo y las últimas líneas del error; no corregir.
4. `rtk gh pr merge <n> --rebase`; luego `rtk git switch main && rtk git pull --ff-only`; `rtk proxy git diff standard/reglamento-1.1.0 main` debe salir vacío.
5. `rtk git tag standard-v1.1.0` (ligero, como los anteriores) sobre `main` y `rtk git push origin standard-v1.1.0`.
6. `rtk gh run watch` del workflow `standard-release` de ese tag hasta terminar en verde; `rtk gh release view standard-v1.1.0 --json assets --jq '.assets[].name'` lista los 3 archivos. Descargar en una carpeta temporal con `rtk gh release download standard-v1.1.0 --dir <temporal>` y comprobar `rtk proxy shasum -a 256 -c SHA256SUMS` = OK.
7. No tocar ningún otro repositorio ni `~/.forge614`.

**Resultado R2 (2026-09-25):** sesión `47be6bba` (Sonnet 5 low), a la primera. PR #12 con `verify` y los tres `parity` en verde, fusionado con rebase a las 15:38Z; `main` = `6c79277` (`git diff standard/reglamento-1.1.0 main` vacío); tag ligero `standard-v1.1.0` → `6c79277`; run `standard-release` 36155571099 en verde; release con `standard-1.1.0.tar.gz`, `SHA256SUMS` y `pack-manifest.json`. Verificado por el orquestador: descarga con `shasum -c` OK y la huella publicada (`cf3dd08e…8be0`) es la del puntero `forge614.node.json` en `main`; sin atribuciones en commits ni PR (las menciones del cuerpo son el nombre del asistente `engram/claude-code` de la matriz). CodeRabbit seguía pendiente al fusionar (no es un check requerido). Agente: 0,48 M tokens ≈ $0,24 en 12 min (casi todo espera de CI). **R2 cerrada; reglamento 1.1.0 publicado.** Pendiente del propietario: revalidar `engram/claude-code` y `engram/codex` con los puntos nuevos del checklist antes del 2026-10-25.

## Impacto en el procedimiento de agentes

**Sí.** Este plan es el cambio del procedimiento que Engram 1.7.0 dejó acumulado: 7 puntos nuevos y 5 correcciones en la sección `forge614-engram` del checklist, la fila del refuerzo del contrato del ecosistema, y la revalidación de `engram/claude-code` y `engram/codex` en la matriz antes del 2026-10-25 (el propietario ejecuta los puntos nuevos con cada asistente y registra la fecha).
